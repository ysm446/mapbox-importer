# 変更履歴

形式: 新しいものを上に。日付は YYYY-MM-DD。

## 2026-08-02
- 3D操作：3Dで掴める対象を、開いているサブタブに合わせて絞るようにした。ランドマークのピンは「ランドマーク」タブ、経路探索の発着マーカーは「ルート（OSM）」タブを開いているときだけドラッグできる。これまではロケーションの詳細に入っていればどのタブでもピンを掴めたため、ルートタブで経路探索している最中に誤ってランドマークを動かしてしまうことがあった。あわせて、ランドマークタブを離れるときは地点の配置モードも解除する（他タブの作業中に地点が増えるのを防ぐ）。表示自体はどのタブでも従来どおりで、止めるのはドラッグ操作だけ。`viewer3d.ts` に `setRouteSearchEditable()` を追加し、`main.ts` の `applyEditableForSubtab()` でタブ切替・詳細開閉・3Dビューワ生成時にまとめて反映する。`npx tsc --noEmit` / `npm run build` 確認済み。
- 経路探索：3Dビューにスタート（緑）／ゴール（赤）のマーカーを出し、ドラッグで動かすたびに取り込み済みルート網の上で最短経路を引き直す「経路探索モード」を追加。ルートパネル内に対象種別の選択（既定は道路・歩道・登山道。鉄道／ロープウェイは既定OFF）、モードのON/OFF、発着リセットを置き、結果として距離・累積上り・累積下り・平均勾配・スナップ距離を表示する。3D上では経路を黄でハイライトし、中点に「距離/勾配」ラベルを出す。
  - 探索の実体は `src/renderer/routing.ts`（Three.js・DOM非依存の純粋モジュール）。OSM の way は交差点で同じノードを共有し `out geom` の座標もそのまま一致するため、頂点を量子化キー（1e-7度≒1cm）で束ねるだけで道路網のトポロジーを復元できる。クリップ処理が端点を `a+t*(b-a)` で作る都合で 1ulp ずれ得るので、完全一致ではなく量子化して照合する。立体交差は座標が一致しないので自動的に非連結になる。
  - 発着点は最寄り線分への垂線の足へスナップし、その線分の両端を始点集合／終点集合としてダイクストラ法（CSR隣接＋二分ヒープ）で解く。重みは水平距離で、コスト定義は `RouteGraph.weights` の生成箇所だけ差し替えれば変えられる。経路が無い場合（連結成分が別）は明示的に案内する。
  - 勾配はフル解像度の `heightmap.u16` から算出する。1点ずつの `workspace:sampleElevation` ではu16を毎回読み直すことになるため、読み込み1回でN点返す `workspace:sampleElevations` を追加した。
  - 検証：グラフの weld（1ulpずれ含む）・非連結時のnull・同一線分の直進・迂回より短い側の選択・種別/表示フィルタ・距離勾配の集計・空グラフを17項目のスクリプトで確認。10万ノード規模でグラフ構築58ms、スナップ＋探索9ms。`npx tsc --noEmit` / `npm run build` 確認済み。
- docs：CLAUDE.md を実態に合わせて修正。docs 参照パスを実際の配置（`docs/plan/goals.md` / `docs/plan/plan.md` / `docs/plan/progress.md`）へ直し、ディレクトリ構成に `osm.ts` / `zip.ts` / `shared/mercator.ts` / `i18n.ts` を追記。保存先の記述をルートフォルダ切替とフォルダ構成（`<id>/heightmap.u16` ほか）に更新（いったん「changelog のみ」に書き換えたのは docs/plan/ の見落としで、4ファイル運用ルールへ復元済み）。
- レビュー修正（renderer/main.ts）：ロケーション選択まわりの競合を解消。①`selectItem` にリクエスト連番ガードを入れ、連続クリック時に古い応答が新しい選択を上書きしないようにした。②`onPlaceLandmark` / `onMoveLandmark` / `fetchOsm` は `await` 中に選択ロケーションが変わった場合に結果を破棄し、別ロケーションへの誤保存（データ混入）を防ぐ。③`enterWorkspace` は読み込み失敗時に詳細モードへ入らないようにした。
- レビュー修正（renderer/main.ts）：OSMルート取り込みで、クリップにより同一 way が複数セグメントに分かれた際にルート `id` が重複し、1本削除すると全セグメントが消える問題を修正（採用順の連番を付与）。
- レビュー修正（renderer/main.ts）：等高線グラデーションの Delete/Backspace キー処理が、エディタ非表示時にも発火してキーポイントを黙って削除・保存していた問題を修正（エディタが表示中のときだけ反応）。
- レビュー修正（renderer/main.ts）：bbox の描画・角リサイズ・平行移動ドラッグ中にウィンドウ外でマウスを離すと、ドラッグ状態と `dragPan` 無効化が残る問題を修正（`buttons === 0` 検知で終了処理を実行。終了処理は `finishDraw` / `endCornerDrag` / `endBoxMove` に共通化）。
- レビュー修正（renderer/main.ts）：未捕捉の Promise 拒否を解消（サムネ読込・ロケーション削除・並べ替え保存・テクスチャ書き出し・ランドマーク保存にエラー処理を追加）。F12スクリーンショットがキャンセルされた際にステータスバーが「保存中…」のまま残る問題も修正。
- レビュー修正（renderer/viewer3d.ts）：`disposeGroup` が使用中の衛星テクスチャ（`satelliteTex`）や morph クロスフェード用の旧テクスチャまで破棄していた問題を修正（`keepMaps` 引数で除外。設定切替のたびに大きな衛星テクスチャがGPUへ再アップロードされるヒッチを防ぐ）。
- レビュー修正（renderer/viewer3d.ts）：slide/wipe トランジション完了時（および wipe 進行中）に、グリッド非表示設定でも軸kmラベルが表示されてしまう問題を修正（`isGridObject` は `gridVisible` を優先）。
- レビュー修正（renderer/viewer3d.ts）：`bilinearSample` の u/v を端へクランプし、クリップしていないルートや bbox 外ランドマークが morph 中に NaN 座標（ルート消失・マーカー飛び）になる問題を修正。ルートラベル非表示中に距離モードを切り替えると再表示時に旧モードの距離が残る問題、ホイールで大きく引いたときにファークリップで地形が消える問題、`dispose()` がキャンバス・デバッグ表示をDOMに残す問題も修正。
- レビュー修正（main プロセス）：`rangeMin`/`rangeMax` 指定で正規化した場合にメタへ実測 min/max が保存され、再読込時の標高復元がずれる潜在バグを修正（メタには正規化に実際に使った範囲を保存）。`satellite:fetch` にも地形生成と同じ400タイル上限を追加。`package.json` の `@types/three` を `devDependencies` へ移動。`npx tsc --noEmit` / `npm run build` 確認済み。
- 3D：環境設定「ラベルを画面に対して一定サイズで表示」をルートの距離/勾配ラベルにも適用した。これまで地名ラベルだけが距離に応じたスケール補正を受けており、ONにしてもカーブのラベルはズームで拡縮していた。ルートラベルのスプライトにも設計スケール（`designScale`）を持たせ、補正処理を `applyFixedLabelScale()` に切り出して地名ラベルと共通の基準（画面高×0.022×行数）で計算するようにした。設定項目のラベル文言も「ラベル（地名・距離/勾配）を…」に更新。
- データ分離：ロケーション群の保存先を「ルートフォルダ」として切り替えられるようにした。現在のルートと最近使ったフォルダは `userData/config.json` の `rootDir` / `recentRoots` に保存し、ライブラリタブ上部の「データフォルダ」ボタンから切替・履歴削除・フォルダ選択ダイアログを操作できる。切替時は選択中ロケーションや3Dビューの状態を引きずらないようウィンドウを読み込み直す。ルートが消えていた場合は既定の `data/` へ自動で戻す。IPC は `root:get` / `root:set` / `root:choose` / `root:forget` を追加。
- データ分離：環境設定 `settings.json` をルート配下から `userData/` へ移し、ルートを切り替えても言語・地図スタイル・3D表示設定を保つようにした。旧 `data/settings.json` があれば初回起動時に自動移行する。`landmark-library.json` と `screenshot/` はルートごとに持つため、フォルダ単位で完結してコピー／持ち運びできる。
- データ分離：`data/` の23ロケーションを `E:\sample files\location-viewer\` 配下へコピーして分割（`japanese-history` に関が原1件、`japanese-mountains` に富士山・北岳など日本の地形14件、`swiss` にツェルマット・アルブラ峠などスイスの地形8件）。各ルートへ `landmark-library.json` も配置。全92ファイルを SHA256 で照合して一致を確認。`data/` は現状のまま残してある。
- 型：`countOutsideLandmarks()` で `selectedBbox` をローカル定数に束縛してから null チェックするよう修正。`filter` コールバック内へナローイングが引き継がれず `TS2345`（`BBox | null` を `BBox` に渡せない）が出ていた既存の型エラーを解消。`npx tsc --noEmit` と `npm run build` の両方が通ることを確認済み。
- UI：生成パネル内にあった進捗表示を画面下部の常設ステータスバーへ移動。生成・地形更新・ロケーション読み込み・エクスポート/インポート・F12スクリーンショットの処理中および完了メッセージを全タブから確認できるようにし、メッセージがない場合は「準備完了」/「Ready」を表示する。長い保存先パスはバー内で省略し、`role=status` と `aria-live=polite` で通知内容を支援技術にも伝える。`npm run build` で確認済み。

## 2026-06-13
- ランドマーク：共通ランドマークライブラリのマスターを `assets/landmarks/landmark-library.json` に移し、Git管理できるようにした。起動時は従来どおり `data/landmark-library.json` をローカル作業コピーとして優先し、存在しない場合だけ `assets/landmarks/landmark-library.json` から生成する。配布ビルドにも同梱されるよう `package.json` の electron-builder `files` に `assets/**/*` を追加し、READMEのデータ配置説明も更新した。
- ランドマーク：座標範囲変更後のツェルマットエリア（`data/ws_mpzmsafn_65ca/`）に `ゴルナーグラート`、`ゴルナーグラート駅`、`ローテンボーデン駅`、`リッフェルアルプ駅`、`リッフェルホルン`、`グリンジゼー`、`グリューンゼー`、`モースジゼー`、`ウンターロートホルン` の9件を追加。Gornergrat/Gornergratbahn系の駅ページ、swisstopo/Wikipedia系の山頂ページ、独語Wikipedia系の湖沼ページで座標・標高・周辺文脈を確認し、保存済みDEMで照合した。リッフェルホルンは急峻な山頂のためDEMが約131m低く出たが、外部標高と周辺文脈を優先した。シュテリゼーとホーテリは代表座標が現在bbox外のため未登録。ツェルマットロケーション内のランドマークは20件、共通ランドマークライブラリは396件になった。`data/` はGit管理外。

## 2026-06-12
- ランドマーク：新規追加されたルンガーンエリア（`data/ws_mqaftlu4/`）に `ルンガーン`、`ルンガーン駅`、`旧教会塔`、`ルンガーン湖`、`カイザーストゥールOW駅`、`ヴィラーホルン`、`ギュプフィ`、`オプゼー`、`ルンガーン＝トゥレン山麓駅` の9件を追加。Wikipedia/map.geo.admin.ch/swisstopo系の座標・標高、Lungern-Schönbüel/Obseeの周辺文脈、保存済みDEMを照合した。Obseeと山麓駅は点座標の直接ソースが弱いため、保存済み衛星画像・既存OSM形状・DEMから代表点として登録し `confidence: medium`、その他は `confidence: high` とした。ルンガーンロケーション内のランドマークは9件、共通ランドマークライブラリは387件になった。`data/` はGit管理外。

## 2026-06-08
- 環境設定：背景色カラーピッカーの横に、他の初期化操作と同じ回転矢印の初期値アイコンを追加。クリックすると背景色を既定の `#111417` に戻して即時反映・保存する。`npm run build` で確認済み。
- 環境設定：3Dビュー > 表示・カメラに `背景色` カラーピッカーを追加。既定は従来の背景色 `#111417` のままにし、変更は `backgroundColor` として `data/settings.json` に保存・復元する。3Dビュー生成時と入力中に `TerrainViewer.setBackgroundColor()` で即時反映する。`npm run build` で確認済み。
- UI：右サイドバーのタブ内容スクロールバーが内側に寄っていたため、`#right-pane` の右paddingを外し、表示中 `.rview` の右端にスクロールバーが出るよう調整。タブ見出しと内容には右余白を残し、カード類がスクロールバーに近づきすぎないようにした。`npm run build` で確認済み。
- UI：右サイドバーの縦スクロールをサイドバー全体ではなくタブ内容エリア内で行うよう変更。`#right-pane` を縦flexの固定コンテナにし、`#right-tabs` は固定、表示中の `.rview` だけをスクロール領域にした。`npm run build` で確認済み。
- UI：等高線の初期値ボタンを、カメラ画角リセットと同じ `vp-icon` の回転矢印アイコンボタンに変更。専用の文字ボタン文言を削除し、既存の `初期値に戻す` ツールチップを流用する。`npm run build` で確認済み。
- UI：等高線の単色カラー入力とグラデーションバーの横に `初期値` ボタンを追加。単色は既定色 `#203040`、グラデーションは既定3ストップへ戻し、即時反映・保存する。`npm run build` で確認済み。
- UI：等高線グラデーションのキーポイント削除を、選択中つまみに対する `Delete` / `Backspace` キー操作に変更。追加はバークリックで行えるため、追加/削除ボタンを削除した。入力欄編集中のDelete/Backspaceは通常の編集操作として扱う。`npm run build` で確認済み。
- UI：等高線グラデーションのキーポイントつまみから疑似要素の突起を削除し、ひし形のみのシンプルな見た目にした。`npm run build` で確認済み。
- 3Dビュー：等高線グラデーションのキーポイントドラッグを再修正。ドラッグ中にハンドルDOMを再生成しないようにし、対象ハンドルの位置・入力値・バー背景だけを局所更新することで、ドラッグが即解除される問題を防いだ。ドラッグ終了時にストップを並べ替え、3D等高線更新と設定保存を行う。`npm run build` で確認済み。
- 3Dビュー：等高線グラデーションUIを調整。キーポイントのドラッグ中はバー上の位置だけを更新し、3D等高線の再生成と設定保存はドラッグ終了後にまとめて行うようにした。また、単色/グラデーションの選択に応じて未選択側の設定要素を `hidden` 属性でも確実に非表示にするよう修正。`npm run build` で確認済み。
- 3Dビュー：等高線の色を `単色` / `グラデーション` から選べるようにした。単色は既存の `contourColor` を維持し、グラデーション時は環境設定 > 3Dビュー > 等高線にバー型のキーポイントUIを表示する。バークリックまたは追加ボタンでキーポイントを追加し、ハンドルのドラッグ、位置(%)/色入力、削除で調整できる。設定は `contourColorMode` / `contourGradientStops` として `data/settings.json` に保存・復元し、3D描画では等高線の標高比に応じてストップ間を線形補間して `LineSegments2` の頂点色で表示する。`npm run build` で確認済み。

## 2026-06-07
- スクリーンショット：F12キーで現在のアプリウィンドウをPNGとして `data/screenshot/` に保存できるようにした。Main側で `BrowserWindow.capturePage()` を使い、`screenshot-YYYYMMDD-HHMMSS.png` のファイル名で保存する。Renderer側では保存中の二重実行を抑止し、保存結果を進捗表示に出す。`npm run build` で確認済み。

## 2026-06-06
- ランドマーク：座標範囲変更後の空木岳エリア（`data/ws_mq238ezt/`）に `南駒ヶ岳`、`赤梛岳`、`田切岳`、`摺鉢窪避難小屋`、`仙涯嶺` の5件を追加。国土地理院の主な山岳標高一覧、ヤマレコ地点ページ、PEAKS/山と溪谷オンラインの山小屋情報で座標・標高・周辺文脈を確認し、保存済みDEMで標高整合を確認した。`南越百山` は現在のbbox南端外のため未登録。空木岳ロケーション内のランドマークは12件、共通ランドマークライブラリは378件になった。`data/` はGit管理外。
- ランドマーク：ランドマークタブに `範囲外を削除` ボタンを追加。選択中ロケーションの保存済み heightmap bbox を基準に、範囲外のランドマーク件数を確認してから一括削除し、保存・3D表示・一覧表示を更新する。範囲外がない場合はボタンを無効化する。
- UI：タブの配色を調整。アクティブタブはパネルと同じ `--surface` にし、非アクティブタブはわずかに明るい面色にして、選択中タブがパネル本体につながって見えるようにした。ホバー時は既存の `--control-hover` で軽く反応する。
- ランドマーク：空木岳エリア（`data/ws_mq238ezt/`）に `空木岳`、`空木駒峰ヒュッテ`、`駒石`、`空木平避難小屋`、`木曽殿山荘`、`東川岳`、`熊沢岳`、`檜尾小屋` の8件を追加。国土地理院の主な山岳標高一覧、mikketa/PORTALFIELD、ヤマレコ、Peakbagger（GSI map source）で座標・標高・周辺文脈を確認し、保存済みDEMで標高整合を確認した。既存の `檜尾岳` と合わせて空木岳ロケーション内のランドマークは9件、共通ランドマークライブラリは373件になった。`data/` はGit管理外。
- 名称：プロジェクト名/アプリ表示名を `Location Viewer` に更新。`package.json` / `package-lock.json` の package name を `location-viewer`、Electron Builder の `productName` を `Location Viewer`、`appId` を `com.example.location-viewer` に変更。HTMLタイトル、Electronウィンドウタイトル、OSM取得時のUser-Agent、README見出しと説明文も新名称へ更新。
- OSMルート：取得カテゴリに `ロープウェイ` を追加。`RouteCategory` に `aerialway` を追加し、Overpass で `way["aerialway"~"..."]`（cable_car/gondola/chair_lift/mixed_lift/drag_lift/t-bar/j-bar/platter/rope_tow/magic_carpet/zip_line/goods）を取得、分類、保存できるようにした。OSMタブの取得カテゴリ、3Dビュー表示メニューの種別表示、2D/3Dの線色、設定保存（`showRouteAerialway`）にも対応。
- ランドマーク：浄土平エリアの `高山` と `鳥子平` の座標を再確認して修正。元の `高山`（140.2813, 37.7078）は保存済みDEMが約1249m、元の `鳥子平`（140.2762, 37.7046）は約1322mで、登録標高1805m/1606mと大きく矛盾していた。`高山` はYAMAP高山（福島県福島市）の拡大地図リンク座標（140.2575556, 37.70059444）へ、`鳥子平` はヤマレコ鳥子平の北緯37度42分17秒・東経140度14分53秒を十進化した（140.2480556, 37.7047222）へ修正。修正後の保存済みDEMはそれぞれ約1801m/約1605mで標高と整合。`data/ws_mpsx5vlv/workspace.json` と `data/landmark-library.json` を更新（`data/` はGit管理外）。
- 3Dビュー：ランドマークラベルの標高行を地名行より小さく表示するよう調整。これまで地名と標高は同じスプライト内で同じフォントサイズだったため、ラベルキャンバス生成を行ごとの倍率に対応させ、ランドマークの2行目（標高）だけ約72%に縮小した。ルート距離ラベルやグリッド距離ラベルには影響しない。
- 環境設定：タブ内を `一般` / `Mapbox` / `3Dビュー` に分け、3Dビュー内も `表示・カメラ` / `注釈` / `ルート` / `等高線` の小セクションで整理。等高線セクションに `等高線の色` カラーピッカーを追加し、設定は `contourColor` として `data/settings.json` に保存・復元。既存の等高線間隔はメートル単位の `contourInterval` のまま維持。
- 3Dビュー：表示メニューの並びを調整し、`グリッドを表示` を一番上に移動。
- 3Dビュー：等高線表示を追加。表示メニューに `等高線を表示` チェックを追加し、標高グリッドから指定した標高間隔の等高線を生成して地形表面の少し上に描画する。線は `LineSegments2` / `LineMaterial` で描画し、環境設定 > 3Dビューの `等高線の間隔` セレクト（5m/10m/20m/50m/100m/200m）で縦方向の分割間隔を調整可能。設定は `showContours` / `contourInterval` として `data/settings.json` に保存・復元。
- 3Dビュー：ビューポートの「表示」メニューに `グリッドを表示` チェックを追加。3Dグリッドと端の距離ラベルをまとめて表示/非表示できるようにし、設定は `showGrid` として `data/settings.json` に保存・復元。
- 3Dビュー：ビューポートの「表示」メニューに `地形を表示` チェックを追加。地形（ハイトフィールド）メッシュだけを表示/非表示でき、グリッド・地点・ルート・ラベルは残る。衛星モードやハイト・モーフ中の旧テクスチャメッシュにも同じ表示状態を反映し、設定は `showTerrain` として `data/settings.json` に保存・復元。
- ランドマーク：検証済みの `data/landmark-library.json` から、生成時トレース用に付与していた `sourceWorkspaces` と `sourceLandmarkIds` を全365件から削除。ライブラリ本体には `source` / `confidence` / `notes` を残し、出典・検証状況・注記で運用する形に整理。対応して `LandmarkLibraryEntry` の任意フィールド定義も削除。`data/` はGit管理外。
- ランドマーク：アルブラ峠・ベルニナ峠・アイガー北壁・フルカ峠・ユリア峠・ゴッタルド峠・ツェルマット由来の `confidence="medium"` 項目122件を検証し、対象エリア由来の項目をすべて `high` に更新。Jungfrau公式、Graubünden Tourism、Bernina Glaciers/Diavolezza-Lagalb公式、Switzerland Tourism/Goms、Zermatt/Matterhorn Paradise公式、Sasso San Gottardo/Gotthard観光系ページ、swisstopo/Wikipedia系の峠・山頂・湖沼・鉄道/駅ページなどで同名地点・周辺文脈・標高を確認し、保存済みDEMとも照合。マッターホルンの急峻な山頂、アイガーヴァント駅の壁中施設、氷河・峠道路・谷筋・索道/駅代表点などDEM差が大きく出る地点は外部標高と周辺文脈を優先した。`data/landmark-library.json` の対象項目は `source` と `notes` を検証済み文言に更新。`data/` はGit管理外。
- 3Dルート：距離/勾配ラベルの距離計算方法を環境設定で切り替えられるようにした。右ペインの環境設定 > 3Dビューに「ルート距離の計算」プルダウンを追加し、`水平距離`（既定、従来どおり）と `実距離（斜距離）` を選択可能にした。ラベルの距離表示と配置中点は選択方式に応じて再計算し、勾配は従来どおり平均勾配として `Σ|標高差| / Σ水平距離` で計算する。設定は `routeDistanceMode` として `data/settings.json` に保存・復元。
- ランドマーク：浄土平・立山・御嶽山・乗鞍岳・白馬岳・上高地・北岳・谷川岳由来の `confidence="medium"` 項目142件を検証し、対象エリア由来の項目をすべて `high` に更新。浄土平ビジターセンター/フィールドガイド、YAMAP、上高地公式、南アルプス市/山小屋公式系ページ、乗鞍岳頂上小屋、御嶽山ビジターセンター/自治体・山小屋系ページ、谷川岳ロープウェイ/山岳ルート情報などで同名地点・周辺文脈・標高を確認し、保存済みDEMとも照合。谷底・雪渓・急峻な稜線・池沼/湿原/山小屋代表点などDEM差が大きく出る地点は外部標高と周辺文脈を優先した。`data/landmark-library.json` の対象項目は `source` と `notes` を検証済み文言に更新。`data/` はGit管理外。
- 3Dランドマーク：マーカーが地名ラベルより前面に描画されることがある問題を修正。地点マーカー・リーダー線・ラベルの `renderOrder` を整理し、重なった場合はラベルが最前面、マーカーがその下、リーダー線がさらに下になるよう調整。
- ランドマーク：富士山・茶臼岳・木曽駒ヶ岳由来の `confidence="medium"` 項目55件を検証し、対象エリア由来の項目をすべて `high` に更新。富士登山オフィシャルサイト、YAMAPの山情報/ランドマーク、那須ロープウェイ公式、とちぎ旅ネット、中央アルプス駒ヶ岳ロープウェイ公式などで同名地点・周辺文脈・標高を確認し、保存済みDEMとの大きな矛盾がないことを確認。`data/landmark-library.json` の対象項目は `source` と `notes` を検証済み文言に更新。`data/` はGit管理外。
- ランドマーク：八ヶ岳・天狗岳由来の `confidence="medium"` 項目27件を検証し、既存の桜平駐車場3件と合わせて同エリア由来30件がすべて `high` になるよう更新。YAMAPの八ヶ岳地図/ランドマーク、北杜市の赤岳ページ、長野県系の山一覧、各山小屋・観光ページで同名地点・周辺文脈・標高を確認し、保存済みDEMとの大きな矛盾がないことを確認。`data/landmark-library.json` の対象項目は `source` と `notes` を検証済み文言に更新。`data/` はGit管理外。
- ランドマーク：既存 `data/*/workspace.json` の21ロケーションに保存されているランドマークを集約し、`data/landmark-library.json` を365件に再生成。同名かつ約80m以内の地点は同一項目としてマージし、既存の桜平駐車場3件は出典・confidenceを保持。それ以外は `source="Imported from existing local location data"` / `confidence="medium"` として、外部再検証前のローカル既存データ由来であることを明示。各項目に `sourceWorkspaces` と `sourceLandmarkIds` を付与し、型定義にも任意フィールドとして追加。
- ランドマーク：共通ランドマークライブラリを追加。`data/landmark-library.json`（初期データは桜平駐車場 上/中/下）を読み込み、ファイルが無い環境では同内容を自動作成する。`LandmarkLibraryEntry` と `Landmark.libraryId` を追加し、選択中ロケーションのbbox内かつ未取り込みの候補を `landmarkLibrary:candidates` IPC で返す。重複判定は `libraryId`、固定ID、同名かつ80m以内。ランドマークタブに「ライブラリから取り込み」ボタンと候補数表示を追加し、取り込み時は対象ロケーションの `heightmap.u16` から標高を再サンプリングして保存する。
- ランドマーク：八ヶ岳ロケーション（`data/ws_mpsvp97w/`）に「桜平駐車場（上）」と「桜平駐車場（中）」を追加。天狗岳ロケーションで確認済みの座標をコピーし、八ヶ岳側の保存済みDEMで上=`138.33611,36.01`（約1919.9m）、中=`138.328956,36.010303`（約1844.7m）を確認。どちらも八ヶ岳bbox内。
- ランドマーク：天狗岳ロケーション（`data/hm_mps28ig3/`）に「桜平駐車場（中）」と「桜平駐車場（下）」を追加。YAMAPモデルコースのGoogle Mapリンク座標を基準にし、登山口駐車場ガイドの位置関係・標高（上/中/下、上20台・中60台・下70台、中1840m・下1630m）と保存済みDEMを照合。追加座標は中=`138.328956,36.010303`（DEM約1845m）、下=`138.315039435402,36.0147896423738`（DEM約1630m）。`data/` はGit管理外。
- OSMタブ：`OSMから取得`、`種別を再判定`、`全ルートをクリア` を同じ操作列に配置。再判定/全クリアをルート一覧の下から取得ボタン横へ移動し、横幅が足りない場合は自然に折り返すよう `.route-actions` を追加。
- 位置選択（不具合修正）：ズーム解像度 z15 で `Terrain-DEM` の標高タイル取得が HTTP 404 になる問題を防止。`Terrain-DEM` の上限を z14、`Terrain-RGB` の上限を z15 として UI スライダーへ起動時から反映し、ロケーション選択時は保存済み `heightmap.sourceId` を復元してからズームを丸めるようにした。Main 側の `TileSource` に `maxZoom` を追加し、上限超過はダウンロード前に分かりやすいエラーで止める。
- 位置選択：生成パネル先頭の「Mapbox Heightmap Importer」見出しを削除。位置選択タブと操作項目だけで文脈が分かるため、地図上オーバーレイの情報密度を下げ、地図表示領域を少し広く使えるようにした。未使用になった `h2` の共通スタイルも削除。
- 位置選択：タイル境界を薄いグリッドで視覚化する「グリッド」トグルを追加。選択中のズームレベルから Web Mercator タイル境界を計算し、現在の地図表示範囲ぶんだけ GeoJSON line レイヤーとして重ねる。パン・ズーム・スタイル切替・ズームスライダー変更に追従し、広域表示で線数が多すぎる場合は最大約220本に間引いて描画する。表示状態は `showTileGrid` として `data/settings.json` に保存・復元。
- 位置選択：ロケーション切り替え時の地図フィット余白を調整。`map.fitBounds` の padding を固定 40px から、地図キャンバスの短辺に応じた 72〜140px に変更し、選択矩形が画面いっぱいに詰まらず少し引いた表示になるようにした。
- 運用ルール：`AGENTS.md` に「作業ごとに docs/ を更新する」最重要ルールを追加。まとまった作業後は `docs/progress.md` の最終更新日と `docs/changelog.md` の当日作業内容を必ず更新し、要件や方針変更時のみ `docs/goals.md`、ロードマップの着手・完了時に `docs/plan.md` を更新する方針を明文化した。

## 2026-06-04
- ロケーションの **ZIP バックアップ書き出し／取り込み（復元）** を追加。外部依存なしの最小 ZIP 実装（`src/main/zip.ts`、Node 標準 `zlib` の deflate/inflate＋自前 CRC32、Zip64 非対応）を新設。`library.ts` に `readWorkspaceEntries`（`data/<id>/` の workspace.json/heightmap.u16/preview.png/satellite.png をフラットに収集、satellite は任意）と `importWorkspaceEntries`（workspace.json/heightmap.u16 必須を検証→**新しい id を採番**して衝突回避・order は末尾・既知ファイル名のみ書き出して zip slip 回避）を追加。IPC `workspace:exportZip`（保存ダイアログ→ZIP 書き出し）／`workspace:importZip`（開くダイアログ→新規ロケーションとして復元）と preload `exportZip`/`importZip` を追加。UI は詳細パネルに「ZIP 書き出し」（選択時のみ有効）、ロケーション一覧ヘッダに「📦 取り込み」ボタンを追加（取り込み後に一覧を再描画）。i18n に `btn.exportZip`/`btn.importZip`/`import.done`/`import.failed` を追加。
- 3Dビュー（最適化）：距離/勾配ラベルが**OFF のときは作らない**ようにした。`renderRoutes` のラベル生成を `routeLabelsVisible` で囲み、OFF（既定）ならワークスペース切替のたびに全ルート分の `CanvasTexture` を生成していた無駄を解消（重いのは計算より1本ごとのテクスチャ生成）。`setRouteLabelsVisible` は OFF→ON で未生成なら `renderRoutes` で作り直す。
- 3Dビュー（不具合修正）：カーブの勾配が**急坂でも 1% 等になる**問題を修正。原因は「上りのみ加算」で、OSM ラインの向き（頂点の並び順）が下り向きだと総上り≈0 になっていたこと。勾配を **Σ\|標高差\| / Σ水平距離（上り下りを絶対値で合算）** に変更し、向き非依存にした（純登り/純下りは実勾配に一致、アップダウンはならした急さを表す）。
- 3Dビュー：**カーブ単位の距離/勾配ラベル**を追加（例「1.4km/18%」を線の中点に小さめフォントで表示）。距離＝水平距離、勾配＝②平均グレード＝`総上り/水平距離`。`renderRoutes` のドレープ計算と同じパスで各ルートの距離/勾配/中点アンカーを算出し、`makeLabelSprite` で routeGroup の子スプライトを生成（ディスク保存なし。地形更新・ルート変更で作り直し）。重なり回避は地名ラベルの `declutterLabels` に相乗り（`placed` 矩形を共有して相互に避ける）＋ オンスクリーン長 LOD（画面上のカーブ長が `ROUTE_LABEL_MIN_PX` 未満は非表示）＋ 優先度＝長い順。「表示」メニューのルート種別の下に「距離・勾配」チェック（`chk-show-route-info`、既定OFF）を追加し `setRouteLabelsVisible`／`showRouteInfo` で保存・復元。種別フィルタ・ルート表示OFFにも追従（declutter が opacity で反映）。`declutterLabels` を地名/ルート両対応に再構成（どちらか一方だけでも動作）。
- 3Dビュー（不具合修正）：「奥を隠す」モードで、モーフ後の自動フィット/カメラ移動中に後ろのラベルが一瞬だけ不透明度1に見えることがある問題を追加修正。`hideFar` のヒステリシスを距離差が小さい場合だけ効かせ、明確に前後が付いたときはカメラ距離順を優先するようにした。あわせてモーフ終了直後の短時間はラベル/リーダー線 opacity を最終判定へ即時反映し、フェード補間による明るい跳ねを抑制。
- 3Dビュー（不具合修正）：「奥を隠す」モードで、モーフ後の自動フィット等の**カメラ移動中に後ろのラベルが一瞬不透明度1になる**問題を修正。原因は貪欲法の前面割り当てがカメラ移動で距離順入れ替わりにより揺れること。`declutterLabels` の hideFar に**ヒステリシス**を導入（`labelShown` を持ち、直前フレームに前面だったラベルを優先処理して座を保たせる）。これで距離順が僅かに前後しても前/後ろが揺れず、後ろのラベルが明るく跳ねない。
- 3Dビュー：「上へ逃がす」モードでラベルが目標位置へ移動する速度をゆっくりに（stem 補間係数 0.15→0.08、`LABEL_STEM_LERP` として定数化）。
- 3Dビュー（不具合修正）：「奥を隠す」（前後で透明度調整）モードで、ハイト・モーフ完了直後にラベルが**一瞬明るくなる**問題を修正。原因はモーフ中にラベル位置が動くと前/後ろ（dim）判定が毎フレーム変わり、完了後に `fadeObject` が最終値へ追いつく過程で明るさが揺れていたこと。対策として (1) declutter の前後判定を**最終位置（`marker.userData.morphNewBase`）基準で固定**（配置は現在位置 `placeBase` のまま追従）、(2) モーフ中は opacity を `fadeObject(..., snap=true)` で**即時設定**（フェード遅延をなくし、完了時に既に最終値）。
- 3Dビュー（演出）：**ハイト・モーフ中もラベルの重なり回避（declutter）を走らせて統合**。これまでモーフ完了後に declutter が動き出して「急に避ける」不自然さがあったのを解消。モーフ中は地形グループが原点のままで画面投影が成立するため declutter をそのまま適用でき、モーフのフェードインは `labelFade`（0→1）を declutter のラベル/線 opacity に乗算して両立させる（`updateTransition` の morph 分岐はマーカー位置と `labelFade` のみ設定し、線・ラベルの配置/不透明度は `declutterLabels` に一元化）。`animate` は `!this.trans || isMorph` で declutter を呼ぶ（slide/wipe はグループが X 移動して投影がズレるため従来どおり対象外）。`finishTransition` で `labelFade` を 1 に戻す。
- 3Dビュー（演出）：ハイト・モーフ中の**地点と軸ラベル(km)を「非表示→完了時に一括表示」から「地形変形に追従＋フェードイン」に変更**（パッと出る違和感を解消）。`startMorph` で軸ラベルの基準位置（`AxisFollow`）と、地点の旧/新接地点（`LandmarkFollow`：新しい注釈の (u,v) を旧起伏 `oldY`・旧フットプリント rx/rz・旧海抜 baseY0 上に合成）を作成。`updateTransition` の morph 分岐で、軸ラベルは grid と同じ rx/rz 比で X/Z を縮めつつ opacity 0→1、地点（マーカー＋リーダー線＋ラベル）は旧→新の接地点へ補間しつつ opacity 0→1。`finishTransition` で新位置・不透明度1へ確定（以後 declutter が引き継ぐ）。マーカー材質を `transparent:true` 化。従来の `hidden`（まるごと非表示）機構は撤去。
- ルート配色：種別ごとの色相差を狭め、**緑寄り（ティール緑→緑→黄緑）のレンジ**に統一。道路=ティール(#2fc7a8)／歩道=緑(#57c860)／登山道=黄緑(#9acd32, 元の色)／鉄道=くすんだ緑(#8fb89a)。2D（`ROUTE_COLORS`）と 3D（`ROUTE_COLORS_3D`）を同じ値で更新。（当初は青→緑にしたが、緑側へ寄せ・登山道は元の黄緑へ戻す調整）
- 3Dビュー：地名ラベルの重なり回避プルダウンの並びを「なし」を一番上に変更（既定は引き続き「上へ逃がす」＝stack に `selected` 付与）。
- 3Dビュー：「奥を隠す」で手前と重なって隠れる**後ろのラベルを完全に消さず薄表示（不透明度20%, `LABEL_DIM_OPACITY`）**に変更。画面外のラベルは従来どおり 0。リーダー線もラベルと同じ濃さでフェード。
- 3Dビュー：地名ラベルの出現/消滅を**不透明度フェード**でスムーズにした。これまで `visible` のハード切替だったのを、`declutterLabels` でラベル（スプライト）とリーダー線の opacity を目標（表示=1／非表示=0）へ毎フレーム補間（`fadeObject`、係数0.18）、ほぼ0で描画停止。hideFar で隠れる/現れるラベルも、stack で画面外に出る/入るラベルもフェードする。リーダー線マテリアルを `transparent:true` 化（フェード対応）。
- 3Dビュー：地名ラベルの重なり回避に「**なし**」を追加（回避せず基準の高さで全ラベルをそのまま表示。`declutter.none`）。
- 3Dビュー：地名ラベルの**重なり回避方式を選べる**ようにした（環境設定 > 3Dビュー > 「地名ラベルの重なり回避」）。`stack`=従来どおり奥のラベルを上へ逃がして全部見せる／`hideFar`=重なったらカメラから遠い（奥の）ラベルを隠して手前を優先（リーダー線も一緒に隠す）。`viewer3d.ts` の `declutterLabels` を方式で分岐し、`setLabelDeclutter` を追加。状態は `labelDeclutter` として保存・復元（既定=stack）。i18n に `settings.labelDeclutter`/`declutter.stack`/`declutter.hideFar` を追加。
- 3Dビュー（最適化）：軸ラベル（グリッド端の km 表示）の**遮蔽判定レイキャストを間引き**。これまで毎フレーム、軸ラベル4個ぶん地形メッシュ全体へレイキャスト（BVH 無し＝O(三角形数)）していたのを、カメラ（位置・注視点）が前回から動いていないフレームはスキップし、動いている間も約80msごとに間引くようにした（`occlDirty`/`occlLastTime`/`occlLastCam`/`occlLastTarget`）。新しい地形に切替時は一度やり直す。閲覧中（カメラ静止）の常時コストが実質ゼロになり、大きな地形ほど効果が大きい。
- 3Dビュー（不具合修正）：ルート統合後にハイトモーフが効かなくなっていた問題を修正。統合 `LineSegments` のジオメトリ position 配列と morph の終点 `newPts` が同一参照だったため、補間初回で終点が上書き破壊され、ルートが旧位置に潰れたまま戻らなかった。morph 時は position に `newPts.slice()`（複製）を渡し、終点バッファを不変に保つようにした。
- 3Dビュー（最適化）：ルートの 3D 描画を**種別ごとに 1 本の `THREE.LineSegments` へ統合**し、draw call をルート本数ぶん → 種別ごと最大4本に削減（カーブ表示時の重さの主因が「線オブジェクト1本=1 draw call」だったため）。`renderRoutes` はポリラインを隣接ペア（線分）に分解して種別別バッファ（new=新地形上／old=morph 用の旧地形上）へ詰め、`LineSegments` を生成。種別表示トグルは統合オブジェクトの `visible` を切替、ワイプのクリップ平面・モーフ補間（`applyRouteMorph`／`RouteMorphLine`）も統合バッファ単位で処理。デバッグ表示のルート行を `src（元の本数）/ draws（描画オブジェクト数）/ verts` に変更。
- 3Dビュー：「表示」メニューに**デバッグ**チェックを追加。ON で左下（ギズモの上）に FPS・draw calls・polygons（三角形数）・geometries/textures・地形頂点数・ルート（表示中の線数／頂点数）を重ねて表示する。`viewer3d.ts` に `setDebug` と `updateDebugPanel` を追加し、`animate` で約0.5秒ごとに FPS を集計、`renderer.info`（render.calls / render.triangles / memory）から統計を読む。状態は `showDebug` として保存・復元（既定=OFF）。オーバーレイ用 `#viewer3d-debug` スタイルと i18n `view3d.debug` を追加。
- 3Dビュー：ルート折れ線を**ワイプ**と**ハイトモーフ**の演出に追従させた。これまでルートは setData の後に setRoutes で作り直されるため、ワイプのクリップ平面が適用されず（線が消えずに残る）、モーフでは地形が変形してもルートが新地形の最終位置に貼り付いたまま浮いていた。`renderRoutes` を進行中の演出（`this.trans`）に対応させ、(1) wipe では新地形側のクリップ平面 `newPlane` を各ルート線マテリアルに共有し `newMats` に登録して seam で一緒にワイプ、(2) morph では各頂点の旧地形上の対応点（旧フットプリント rx/rz・旧起伏を `oldY` からサンプル・旧海抜 `baseY0`）を作り、`applyRouteMorph` で旧→新へ毎フレーム補間。morph 状態に `cols`/`rows`/`routeLines` を追加し、`updateTransition`/`finishTransition` でルートも更新・確定。slide は従来どおりルートが地形グループの子として一緒に動く（変更なし）。旧地形側のルートも従来どおりクリップでワイプアウトする。

## 2026-06-03
- OSM ルート：種別「歩道（path系まとめ）」を**歩道（foot）**と**登山道（trail）**に分割。`RouteCategory` を `'road' | 'foot' | 'trail' | 'rail'` に変更。判定基準は OSM の highway 種別＋`sac_scale`：footway/pedestrian/steps/cycleway=歩道、path/bridleway=登山道、`sac_scale`（登山難易度）が付く歩道系は登山道へ寄せる（`osm.ts` の `classify`／`buildQuery`）。配色は歩道=オレンジ(#ffa726)、登山道=黄緑(#9acd32) に分離（2D `ROUTE_COLORS`・3D `ROUTE_COLORS_3D`）。OSM 取得パネルと「表示」メニューのチェックを歩道/登山道の2つに分割。i18n に `route.catFoot`/`route.catTrail` を追加。
- OSM ルート：保存済みルートの**種別を再判定**する機能を追加。ルートは OSM タグを保持しないため、`osmId` を Overpass に問い合わせ（`osm.ts` の `fetchOsmTagsByIds`：`out tags` でジオメトリ無しの軽量取得）、`classify` で歩道/登山道/道路/鉄道に振り分け直す。IPC `osm:reclassify`（`getWorkspace`→タグ取得→分類→`saveRoutes`、更新本数を返す）と preload `reclassifyRoutes` を追加。ルートパネルに「🔄 種別を再判定」ボタン（OSM 由来ルートがあるときだけ表示）。クリップ・除外・改名はそのまま保持。旧種別 `'path'` は読み込み時に `'foot'` へ寄せ、再判定で正しく振り分ける（`library.ts`）。
- 3Dビュー：「表示」メニューの「ルートを表示」の下に種別ごとの子チェック（道路 / 歩道 / 登山道 / 鉄道）を追加。種別単位で 3D ルートの表示/非表示を切り替えられるようにした。`viewer3d.ts` に `routeCatVisible` と `setRouteCategoryVisible(cat, on)` を追加し、各 `THREE.Line` の `userData.routeCategory` と `line.visible` で種別フィルタを反映（`renderRoutes` でも復元）。`main.ts` は3つのチェック（`chk-show-route-road/path/rail`）を配線し、状態を `showRouteRoad`/`showRoutePath`/`showRouteRail` として保存・復元。「ルートを表示」OFF 時は子チェックを淡色＆無効化（`.popup-submenu.disabled`）。`AppSettings` に3キーを追加。インデント表示用の `.popup-submenu` スタイルを追加。

## 2026-06-02
- OSM ルート：自動車道（road カテゴリ）のライン色をオレンジ（#ff8c2b / 0xff8c2b）から水色（#4fc3f7 / 0x4fc3f7）に変更。2D（`main.ts` の `ROUTE_COLORS`）と 3D（`viewer3d.ts` の `ROUTE_COLORS_3D`）を一致させた。
- OSM ルート：歩道・登山道（path カテゴリ）のライン色を緑（#36b34a / 0x36b34a）から黄緑（#9acd32 / 0x9acd32）に変更。2D 地図（`main.ts` の `ROUTE_COLORS`）と 3D ドレープ（`viewer3d.ts` の `ROUTE_COLORS_3D`）の両方を更新し、配色を一致させた。
- OSM ルート：「全ルートをクリア」ボタンを追加（ルートタブ、一覧の下）。保存済みの全ルートを確認ダイアログののち削除し、2D/3D とパネルを更新（候補は対象外）。ルートが1本以上あるときだけ表示。クリップ（エリア外カット）は新規取得にしか効かないため、既存ルートをまとめて消して取り直すための導線。控えめな破壊的アクション用に `.link-danger` スタイルを追加。
- OSM ルート：選択範囲（bbox）からはみ出した部分をカットするオプションを追加。Overpass は bbox にかかる way の全体形状を返すため、`osm.ts` に Liang–Barsky による線分クリップ（`clipSegment` / `clipPolylineToBBox`）を実装し、bbox を出入りするラインは矩形内の連続区間ごとに分割して返す。ルートタブに「エリア外をカット」チェックボックス（既定ON）を追加し、`fetchOsmRoutes(bbox, cats, clip)` 経由で取得時に切替（IPC/preload に clip 引数を追加）。
- UI：ロケーション詳細（右ペインのワークスペース内）を「ランドマーク」「ルート（OSM）」のサブタブに分割。これまで縦に積んでいた地点UIとルートUIをタブ切替式にし、`#ws-subtabs`（`.tab`）＋`#subview-landmarks` / `#subview-routes`（`.subview`）で表示を切り替える（`showWsSubtab`）。詳細を開くたびにランドマークタブから開始。地点の「ランドマーク」見出しとルートの「ルート（OSM）」見出しはタブ名が兼ねるため重複ラベルを削除。
- OSM ルート（フェーズB・ドレープ）：保存済みルートを 3D 地形にも沿わせて描画。`viewer3d.ts` に `setRoutes` / `setRoutesVisible` / `renderRoutes` を追加し、各折れ線の頂点を lng/lat→ローカルXZ に変換、表示メッシュ表面（`surfaceWorldY`）＋数メートルの浮きで `THREE.Line` を生成（種別色は 2D と一致：道路=オレンジ/歩道=緑/鉄道=グレー）。ルートグループは地形グループの子にしてスライド/ワイプ/モーフ演出で地形と一緒に動く。`main.ts` はルートの読込/取得保存/表示・改名・削除のたびに `viewer.setRoutes(routes)` で 3D を同期。3Dビューの「表示」メニューに「ルートを表示」トグルを追加（`showRoutes` として保存・復元）。※区間距離(km)・勾配(%)ラベルは未実装（次の一手）。
- OSM ルート（フェーズA）：選択中ワークスペースの地形 bbox を使い、OpenStreetMap（Overpass API）から道路（highway）/歩道・登山道（footway/path）/鉄道（railway）のラインを取得して 2D 地図にオーバーレイする仕組みを追加。メインに `src/main/osm.ts`（Overpass QL 組み立て・カテゴリ分類・lng/lat 折れ線化、タイムアウト/上限8000本）を新設し、IPC `osm:fetch` / `workspace:saveRoutes` と preload API（`fetchOsmRoutes` / `saveRoutes`）を追加。データモデルは `Workspace.routes`（`Route`: id/name/category/osmId/coords/visible）を追加し `workspace.json` に保存（旧構成は空配列で移行）。Overpass はリクエストに `User-Agent` が無いと 406 を返すため、`User-Agent` と `Accept: application/json` を付与（実データで 406→200 を確認）。UI はライブラリの選択ワークスペース内に「ルート（OSM）」セクション（種別チェック＝道路/歩道・登山道/鉄道、取得ボタン、採用保存ボタン、一覧）を追加。取得後は候補ラインを地図にカテゴリ別配色で重ね、要らない線をクリックで除外（薄いグレー表示）→「採用を保存」で `routes` に確定・保存。保存済みルートは選択時に再描画、一覧で表示/非表示・改名・削除が可能。スタイル切替（styledata）でも再構築。※3D 地形へのドレープ＋距離/勾配ラベルはフェーズBとして未実装。
- 3Dビュー：「表示」メニューに「ヘルプ」チェックを追加し、右下のマウス操作ガイド（#viewer3d-help）を表示/非表示できるようにした。状態は `showHelp` として保存・復元（既定=表示）。
- 3Dビュー：自動回転ボタンを削除し、O キーのトグルに変更（入力欄フォーカス中や修飾キー併用時は無効）。操作ヒントに「O=自動回転」を追記。
- 3Dビュー：ビューポート右上の操作バーに「表示 ▾」ボタンを追加し、クリックでチェック項目のポップアップメニューを開閉。これまでバーに直置きだった「地点を表示」チェックボックスをこのメニューへ移動。外側クリック/Escape で閉じ、メニュー内のチェック操作では閉じない。今後の表示トグル追加の受け皿にもなる。
- 3Dビュー：カメラのスムーズズームをゆっくりに（updateSmoothZoom の補間係数 0.2→0.12）。
- UI：左タブ「位置選択」の世界地図アイコン（🗺）を削除（HTML フォールバック＋i18n 日英）。
- UI：右ペイン（ライブラリ/環境設定）の幅をマウスドラッグで変更できるようにした。`#main-pane` と `#right-pane` の間にリサイザー `#right-resizer`（col-resize、hover/ドラッグ中はアクセント色）を追加し、pointer イベントで `flex-basis` を 240〜760px の範囲で調整。ドラッグ中は地図を `map.resize()` で即追従（3Dビューワは ResizeObserver で自動）、確定時に幅を `rightPaneWidth` として保存し次回起動時に復元。
- 3Dビュー：自動フィット（ロケーション切替時にフィット）が切替トランジションと同時にズームしていたのを、演出完了後にズームするよう変更。setData では near/far のクリップ範囲だけ先に広げ（演出中の見切れ防止）、カメラ移動は演出中なら `pendingFitDist` に保留して `finishTransition` で `zoomTarget` に反映。保留中は進行中ズームを止める（割り込み切替で古い目標へ寄らないように）。演出なし（none）のときは従来どおり即フィット。
- 3Dビュー：地名ラベルのマーカー（ポイント）とリーダー線が地形の深度を無視して常に最前面に描かれていたのを修正。深度テストを有効化し、手前の地形に正しく隠れるようにした。マーカーは自分が乗っている地面とのZファイト/めり込みを避けるため polygonOffset（-2）で少し手前にバイアス。線はラスタ上 polygonOffset が効かないため深度テストのみ（根本はマーカーが覆う）。ラベル文字（スプライト）は従来どおり最前面表示のまま。あわせてマーカー半径を 0.01→0.007 に縮小。
- 3Dビュー：環境設定に「地名ラベルを画面に対して一定サイズで表示（ズームで拡縮しない）」チェックボックスを追加（`fixedLabelSize` / `setFixedLabelSize`）。ON のとき declutter で毎フレーム、ラベルのワールドスケールを距離に応じて補正し、画面高に対する一定比率（行数ぶん）の見かけサイズを維持する。対象は地名ラベルのみ（マーカー・線・軸ラベルは従来どおり）で、scaleAnnotations とは独立＝両方ONでもラベルだけ画面固定。ラベル作成時に設計スケール（`designScale`）と行数（`lines`）を控えて基準にする。
- 3Dビュー（修正）：海抜オフセット ON のモーフ切替で、開始時に地形が一瞬新しい海抜位置（原点寄り）へ飛んでからモーフしていた問題を修正。新メッシュの縦オフセット `mesh.position.y` を旧地形の海抜（旧 minEle*k）から新地形へ毎フレーム補間し、現在の座標から滑らかに上下するようにした（旧テクスチャ用 oldTexMesh も同期）。
- 3Dビュー：モーフ切替中にグリッドも一緒に変形するように変更。従来は変形中グリッドを隠して完了時に出していたが、地形のフットプリント補間（旧/新の東西比 rx・南北比 rz）に合わせてグリッドを X/Z 伸縮させ、完了時に等倍へ戻す。軸ラベル（km 表示）は途中で実寸とズレるため従来どおり変形中は非表示のまま。
- 3Dビュー（修正）：海抜オフセット ON ＋衛星モードでモーフ切替するとメッシュが二重に見える不具合を修正。旧テクスチャ用に同じ変形ジオメトリを共有する `oldTexMesh` に本体メッシュの縦オフセット（`mesh.position.y`）をコピーしておらず、持ち上げた本体と y=0 の旧テクスチャメッシュが上下にずれていた。`oldTexMesh.position.copy(mesh.position)` を追加。
- 3Dビュー：環境設定に「海抜標高で配置（海面=0m を基準に地形を浮かせる）」チェックボックスを追加（`seaLevelBase` / `setSeaLevelBase`）。ON のとき地形メッシュを最低地点ではなく実標高で配置（メッシュを `minEle*k` だけ持ち上げ、`mesh.position.y` に設定）。ジオメトリ自体は base=0 のままにして頂点色・morph 補間は不変、地点マーカーの接地（`surfaceWorldY`）とカメラ注視点（`centerY`）も同じオフセットを反映。グリッド（y=0）が海面基準になり、ロケーション切替（スライド/ワイプ/モーフ）で各地形が実標高の相対位置で並ぶ。ビューポート表示のみで、`.u16`・エクスポートには影響しない。
- 位置選択：地図を立体表示する「3D」トグルを追加（地図ツールバー、矩形描画の左）。Mapbox Terrain-RGB を raster-dem ソースに使い `map.setTerrain`＋pitch 60° に傾ける。スタイル切替後も styledata で terrain を再適用。標高ソースはトークン必須。アイコンは山シルエットのSVG。
- 3Dビュー：切替トランジションに「ハイト・モーフ」を追加（環境設定プルダウン4番目）。旧地形の起伏を新グリッドへリサンプルし頂点Yを旧→新へ補間（頂点色・法線も毎フレーム更新）。横方向（フットプリント）も X/Z スケールで旧→新へ補間。衛星モードでは同一変形ジオメトリを共有する旧テクスチャ用メッシュを重ねて opacity で旧→新テクスチャをクロスフェード。モーフ中は grid・軸ラベル・地点を隠し完了時に表示（plan.md にラベル自然化のメモ）。
- 3Dビュー：ビューポート左上にロケーション名のタイトルを表示（大きめ・細字）。寸法情報はその下へ移動。選択/リネーム/選択解除に連動。
- 3Dビュー：ラベルの重なり回避（declutter）が演出後に急に作動して跳ねる問題を修正。演出開始時に一度 declutter を実行し、新ラベルを最初から回避済み位置に置く。
- 3Dビュー：ワイプ演出のラベルを「クロスフェード」から「境界(seam)同期の表示切替」に変更。各ラベル（軸km・地名スプライト）の x 座標が境界を通過した瞬間に表示/非表示を切り替え、マーカー・線（クリップ平面で同期）と合わせて地点一式が境界通過と同時に出現/消滅するように。境界の振り幅をグリッド端まで含む半幅（`halfExtent`）に拡大して端のラベル/グリッドも確実に出るよう修正。スライドは従来どおり opacity クロスフェード。
- 3Dビュー：ロケーション切替の3Dトランジションを追加（環境設定にプルダウン：なし/横スライド/ワイプ）。旧地形メッシュをワールド空間に生かしたまま、横スライド（旧を左へ・新を右から）またはワイプ（ワールドのクリップ平面で見せ分け）で切替。地点グループを地形グループの子にして一緒に動かし、ラベル（軸km・地名）は前半=旧フェードアウト/後半=新フェードインのクロスフェード。衛星テクスチャは旧メッシュが消えるまで破棄を遅延（`pendingDisposeTex`）、演出中の見た目再描画は完了後に反映（`pendingRedraw`）。地点は `setData(payload, fitCamera, landmarks)` で構築時に渡し、後追いの再構築で新ラベルが差し替わらないように修正。
- docs：レイマーチ・ボリュメトリック雲（影付き）のアイデアを plan.md に保留メモとして追加（重い／DCC寄りになるトレードオフ込み）。
- 3Dビュー：カメラの画角（FOV）を環境設定で調整できるスライダー（20〜90°, 既定50°）と初期値に戻すリセットボタンを追加（`cameraFov` / `setFov`）。視点は維持して投影だけ更新。
- 3Dビュー：自動フィットの距離計算を「縦いっぱい」に変更。東西幅(halfX)を距離に含めず南北＋高さ（`fitDistFor`）で算出、余白(×1.15)を撤廃。画角変更時も自動フィットON中はライブで距離を追従。
- 3Dビュー：地点マーカー（サークル）とラベル（地名）の基準サイズを一回り縮小（マーカー半径 0.013→0.01、ラベル worldH 0.042→0.034）。
- 用語変更：UI 表示名「ワークスペース」→「ロケーション」（i18n 日英 + HTML 静的文言のみ。`Workspace` 型・`workspace:*` IPC・要素IDなど内部識別子は据え置きで挙動不変）。
- 3Dビュー：地点ピンのドラッグ移動を**詳細（地点編集）モードのときだけ**許可するよう変更（誤操作防止）。`viewer.setLandmarksEditable()` を追加し、`showWorkspaceDetail()` の一覧⇔詳細切替に連動。編集モード外ではドラッグ開始も grab カーソルも出さない。3Dタブ遅延生成時は現在の詳細状態を初期反映。編集解除時に進行中ドラッグを安全に打ち切り。
- 3Dビュー：注釈（地点マーカー・リーダー線・ラベル・軸の距離ラベル）を地形スケールに合わせて拡縮するチェックボックスを環境設定に追加（`scaleAnnotations`）。単一倍率 `annotScale`（地形の最大辺ワールドサイズ / 基準2）をマーカー半径・線長・ラベル worldH・declutter 定数に適用。OFF（既定）は絶対サイズ固定。
- 3Dビュー：ワークスペース（ロケーション）切替時にカメラを全体が収まる距離へ自動でフィットするチェックボックスを環境設定に追加（`autoFit`）。寄せ/引き両方。クリップ面（near 縮小のみ / far 拡大のみ）をアニメ中も含め確保。スムーズズーム機構を再利用。
- 3Dビュー：地形ごとの正規化（最大辺→2）をやめ、全地形で共通の固定スケール（`METERS_PER_WORLD_UNIT=5000`, 1ワールド単位=5000m）に変更。ロケーション切替で実サイズの差がそのまま比較できる。
- UI：左の生成パネルを固定カラムから外し、位置選択タブの地図上にだけ出るオーバーレイ化（`#left-pane` を `#view-map` 内へ移動、`.view.hidden` で自動表示切替）。地図ツールバー（`#map-overlay`）はパネルと重ならないよう右上へ移動。
- 環境設定タブ：並び順を「言語 → トークン → 3Dビュー」に変更（3D 関連を重要度的に下へ）。
- 3Dビュー：ワークスペースを切り替えてもカメラをリセットせず視点を維持するように変更。地形は常に同じワールド倍率（原点中心）で表示されるため、`setData` のカメラフィットは**初回データ表示時のみ**実行し、以降は `fitCamera=true` でも視点を保つ（`hasFittedCamera` フラグ）。
- 3Dビュー：ホイールズームをスムーズ化。OrbitControls の即時ズーム（`enableZoom`）を無効化し、ホイールで目標距離を蓄積→毎フレーム `lerp` でカメラ距離を補間する自前のスムーズズームに置き換え（`updateSmoothZoom`）。注視点中心・最小距離0.01で貫通防止。

## 2026-05-31
- 3Dビューポート：自動回転トグルボタンを追加。押すとカメラが縦軸まわりに約2°/秒でゆっくり回転、もう一度押すと停止（OrbitControls の autoRotate）。
- ワークスペース：新規作成時に手動並び順（order）があれば末尾（最大+1）を自動付与。並べ替え後に新規作成しても一覧の並びが作成日順に戻らないように修正。
- ワークスペースタブ：一覧を囲っていたカード枠を外し、項目をタブ内に直接並べるように（`section.card` → `#ws-panel`）。環境設定タブも同様にカード枠を撤去（`.settings-group` で間隔のみ確保）。
- 3Dビュー：グリッド端の方角・距離ラベルが地形の裏に回り込んだら隠す。深度テストだと文字が途中で見切れるため、アンカー点のオクルージョンをレイキャストで判定し all-or-nothing で表示/非表示する方式に（見切れ防止）。
- 3Dビューポートの描画モード・「地点を表示」の状態を `data/settings.json` に保存・起動時復元（`AppSettings` に `renderMode`/`showLandmarks` を追加）。
- 3Dビューポートのオーバーレイ配置を整理：描画モード・地点表示の操作バーを右上へ、操作ヘルプを右下へ、左下はギズモ用に空けた（左上=寸法情報は維持）。
- 3Dビューポート：操作バーに描画モードのプルダウン（デフォルト=地形配色 / ハイトマップ=標高グレースケール / 衛星画像）を追加。衛星チェックボックスは廃止（`setUseSatellite`→`setRenderMode`）。「地点を表示」チェックボックスを右ペインからビューポート操作バーへ移動。
- 3Dビュー：ランドマークのラベルが重なるとき、隠さずに**リーダー線を伸ばして上へずらし重なりを回避**（手前＝カメラに近い側を基準位置に、奥側ほど上へ逃がす。マーカーは地表に固定）。毎フレーム評価。ずらしは目標位置へlerpで**スムーズに移動**（地点ごとに現在長を保持）。
- ワークスペース一覧：1クリック＝選択（地形を表示・一覧は維持）、行末の「›」アイコン（または行ダブルクリック）＝詳細へドリルイン、に分離。クリックだけで中に入らないように変更。新規作成/地形更新時は自動で詳細へ。
- ランドマーク：各地点の行に個別の表示チェックボックスを追加（`Landmark.visible` に保存。非表示の地点は3Dに描画しない）。
- ランドマーク：詳細パネルに「地点を表示」チェックボックスを追加し、3Dの地点表示を一括ON/OFF（非表示中はドラッグ移動も無効）。
- 3Dビュー：ランドマークの根本を「保存標高」ではなく**表示メッシュ表面**に接地させるよう変更（高さグリッドをバイリニア補間）。DEMの平滑化や標高の手入力値に関わらず浮かなくなり、ラベルには実標高を表示できる。GeoContext に cols/rows/heights を追加。
- 3Dビュー：地点（ランドマーク）の地表マーカーをドラッグして移動できるように。掴むと OrbitControls の回転を抑止（capture フェーズで横取り）、地形にレイキャストして lng/lat を更新、離した位置で標高を再サンプリングして保存。
- start.bat：アプリ正常終了時は `pause` せずコンソールを自動で閉じるよう変更（エラー時のみ開いたまま）。アプリを閉じるとプロンプト画面も一緒に閉じる。
- 右サイドバーを2階層ナビゲーションに：ワークスペース一覧 → クリックで詳細モードへ（CSSで選択以外のワークスペースと一覧ヘッダを隠し、選択行の下に選択情報・書き出し・地点リストを並べる）。「← ワークスペース一覧」リンクで戻る。
- データ管理を「ワークスペース」モデルへ再設計：
  - 最上位を**ワークスペース**とし、**ハイトマップ（地形）**と**地点（ランドマーク）**がぶら下がる構造に。地形はワークスペース内で更新（再生成・範囲変更も可）でき、地点は lng/lat 保持なので更新後も残る。
  - 保存レイアウトを `data/<id>/`（`workspace.json` / `heightmap.u16` / `preview.png` / `satellite.png`）に変更。旧フラット構成（`library.json` + `<id>.*`）は**起動時に自動マイグレーション**。
  - IPC を `workspace:create/updateHeightmap/get/list/rename/delete/reorder/thumb/saveLandmarks/sampleElevation/export` に刷新。preload も同名 API に。地形更新時は古い衛星を破棄し再取得。
  - UI：「ライブラリ」→「ワークスペース」表記。左パネルに「新規ワークスペース作成」と「選択中の地形を更新」を分離。一覧に地点数を表示。
- 3D ランドマーク（地点）機能を追加（フェーズ1）：
  - 3Dビューで地形をクリックして地点を配置（レイキャスト）。リーダー線＋ラベル（名前＋標高）を地形に隠れず表示。
  - 右ペイン「ライブラリ」タブに編集パネルを追加（名前・緯度・経度・標高の数値編集／削除）。
  - 座標は lng/lat で `data/<id>.annotations.json` に保存（アイテム単位、遅延読み込み）。標高は配置時に全解像度 u16 からサンプリングし手入力で上書き可。
  - 追加IPC: `annotations:get/save/sampleElevation`。MeshPayload に bbox を追加。`makeLabelSprite` を複数行対応に。
- PNG16 書き出しの形式を変更：従来の 16bit グレースケール（colorType 0）から、**16bit RGBA（colorType 6, R=G=B=標高 / A=不透明）＋ pHYs チャンク**へ。World Machine / Gaea 等の参照ハイトマップ（mountain-ridge サンプル）と同一形式に合わせた。`exportPng16` に CRC32 と pHYs 挿入処理を追加。
  - 注意：値の刻みは元 DEM の垂直分解能（0.1m）に依存するため、合成地形のような完全連続（刻み=1）にはならない（実データ由来）。

## 2026-05-30
- UI：「2のべき乗」チェックボックスを地図オーバーレイから左サイドの生成パネル（範囲入力の下）へ移動。`.checkbox-row` を追加。
- UI：タブの縦余白を縮小（`.tab` の padding を 4px に）。
- UI：背景を無彩色グレー（色味なし）に変更。サーフェス/ボーダー/テキストを寒色なしのニュートラルグレーに。選択行の背景も無彩色にし、選択は枠線（アクセント青）で示す。
- UI：タブを全幅ストレッチ（`flex: 1`）から中身幅・左寄せに変更（だらしない見えを解消）。左右タブ共通。
- UI：ダークテーマを整理（シンプル＆ダーク）。一律ディミング（`--ui-brightness` 80%）をやめ素の配色（100%）に。サーフェスを段階化、ボーダーを2段に統一、テキストは明るく、アクセントは落ち着いた青1色に集約。低コントラストでくすんでいた印象を解消。
- UI：配色をハードコードから CSS 変数（`:root` の設計トークン）へ集約。`--c-*`（元色パレット）＋`--ui-brightness`（UI明度）から `color-mix` で実使用色を導出する方式に。`brightness` フィルタのハックは廃止し、明度は `--ui-brightness`（既定 80%）1か所で調整可能に。地図・2D・3D のコンテンツは CSS 色の影響を受けないため常に元の明るさ。
- ライブラリ：行を左端のハンドル（⠿）でドラッグして表示順を入れ替え可能に。順序は `library.json` の `order` フィールドへ永続化（`library:reorder` IPC / `reorderLibrary` を追加）。手動並べ替え後は新規生成分を先頭に挿入。未並べ替え時は従来通り新着順。
- 3Dビュー：衛星画像チェックボックスの ON/OFF（および衛星テクスチャ適用）でカメラ視点がリセットされる問題を修正。`setData(payload, fitCamera=false)` を追加し、見た目だけの再構築では視点を維持。
- 3Dビュー：「高さ強調」スライダーを削除（常に実寸=1.0 表示）。`setExaggeration`・`exaggeration` フィールド・i18n キー（`view3d.exaggeration`）も併せて削除。
- 3Dビュー：グリッドの中心軸（東西=X / 南北=Z）の端に、中心からの距離(km)を小さめのラベルで表示（E/W/N/S + km）。メッシュ再構築時に作り直し・破棄。
- 位置選択：「表示範囲を選択」ボタンを削除（矩形描画で選択するため不要）。関連の i18n キー（`map.useView`）も削除。
- 位置選択：地図オーバーレイの「地図の種類」プルダウンを一番左へ移動。
- リファクタリング（挙動変更なし）：
  - Web Mercator 座標変換式を `src/shared/mercator.ts` に集約し、メイン（`tiles.ts`）とレンダラー（`main.ts` の解像度推定・スナップ）の重複を解消。`tiles.ts` は互換のため再エクスポート。
  - `heightmap.ts` の `buildMeshData` と `meshFromValues16` のほぼ同一なダウンサンプリング処理を内部ヘルパ `downsampleToMesh`（サンプラ関数で差分吸収）へ共通化。
  - `buildHeightField` の未使用 `tileX0/tileY0`（`void` で握りつぶし）を分割代入から除去。
- ライブラリ：名前変更/削除ボタンをアイコン化（インラインSVG、ツールチップ付き）。
- ライブラリ：空だったサムネ枠に各アイテムの画像を表示（衛星画像優先、なければグレースケールプレビュー。`library:thumb` IPC / `getThumb` を追加）。
- Electron 既定のアプリメニュー（File/Edit/View…）を非表示化（Menu.setApplicationMenu(null)）。
- 右サイドバーをタブ化（ライブラリ / 環境設定）。言語・トークンを「環境設定」タブへ移動。左ペインは生成設定のみに。
- レイアウトを3カラム化：左=設定・生成パネル、中央=ビュー(位置選択/2D/3D)、右=ライブラリ。`#side-pane` を `#left-pane`/`#right-pane` に分離。
- サイドバーの縦スクロールバーを細く（6px、WebKit/Firefox 両対応）。ズームスライダー幅は全幅(100%)に戻した。
- 3D 軸ギズモ：上方向(Up)のラベルを削除（矢印のみ残す）。E/N ラベルは存置。
- 位置選択：選択矩形の四隅にハンドルを表示し、ドラッグで範囲をリサイズできるようにした（反対の角を固定、左右上下の反転も正規化）。
- スライダー（ズーム/高さ強調）を細トラック＋小さい丸つまみにし、幅も縮小。
- 推定出力に選択範囲の地表実距離（横×縦, km, haversine）を追加表示。
- 3D ビュー：グリッド端の方位ラベル(N/S/E/W)と地表の色付き軸線を削除（左下の軸ギズモは存置）。
- 地図ラベルが言語切替で更新されない不具合を修正：`map.setStyle` を `{ diff: false }` で呼び、言語パラメータのみの変更でもラスタタイルを再取得するようにした（diff 更新だと同一ソース ID のタイルがキャッシュされ英語のままだった）。
- UI 多言語化（日本語 / 英語）。右サイドに言語プルダウンを追加し、`data-i18n` 属性ベースで文言を切替（`src/renderer/i18n.ts`）。選択は `data/settings.json` に保存・起動時復元。
- 地図ラベルを UI 言語に連動（タイル URL に `?language=ja|en`。衛星のみは地名なしで影響なし）。
- 3D ビュー：衛星テクスチャの南北反転を修正（不要な UV V 反転を削除。北西原点の画像が地形の北側に正しく貼られるように）。
- 3D ビュー：方位を明示。グリッド端に N/S/E/W ラベルと色付き軸線（東西=赤 / 南北=青）を追加し、左下に向き同期の軸ギズモ（E赤/Up緑/N青）を描画。北=-Z・東=+X の地理対応を可視化。
- 環境設定を `data/settings.json` に保存（地図スタイル）。起動時に復元。`settings:get`/`settings:set` IPC を追加。
- 位置選択：地図スタイル切替を追加（衛星 / 衛星＋地名 / 地図(地名) / 地形図、Mapbox styles/v1 ラスタタイル）。スタイル切替後も bbox 矩形を再描画。選択は `data/settings.json` に保存。
- 位置選択：マウスドラッグで矩形を描いて範囲選択する「矩形を描く」モードを追加（描画中はパン無効・十字カーソル）。
- ライブラリ選択時に対象 bbox へ地図を fitBounds で移動し、ズーム値も復元。
- 推定表示にメガピクセル（MP）と「タイル上限超過」警告を追加。
- 2D ハイトマップビュー：ホイールでズーム（カーソル基点）、ドラッグでパン、ダブルクリックでフィットを実装。
- 衛星画像の保存不具合を修正：Mapbox 衛星タイルは拡張子/Accept に関わらず **WebP** を返すため pngjs 合成が失敗していた。合成を Chromium 側の Canvas（WebP ネイティブ対応）に移動し、`satellite:fetch`（WebP タイル取得）→ レンダラーで Canvas 合成 → `satellite:save`（PNG 保存）の流れに変更。`data/<id>.satellite.png` に保存し 3D メッシュへ貼り付け。
- 衛星画像テクスチャ対応：3D メッシュに貼り付け、UI のチェックボックスで衛星画像/標高配色を切替（`viewer3d.setSatelliteTexture`/`setUseSatellite`）。衛星取得は失敗してもハイトマップ生成は継続。
- 3D ビュー：レンダラーの setSize で updateStyle を有効化（キャンバスが devicePixelRatio 分拡大され内容が左下に寄る不具合を修正）。
- 3D ビュー：初期カメラを地形バウンディングボックスにフィット（内包球の半径と FOV から距離を算出、約30°見下ろし、near/far も自動調整）。地形が下に寄る/オフセットする問題を解消。
- 3D ビュー：高さを実寸化（水平と同じメートル単位でジオメトリを作り、全軸を一様スケール）。高さ強調スライダーを 1.0=実寸 基準（1〜10×）に変更。
- 3D ビュー：地表基準面にグリッドを追加（1マスはきりの良い実距離 1/2/5×10^n m に丸め）。bbox から地表サイズ(m)を haversine で算出し IPC ペイロードに付与。
- 3D ビューの操作変更：マウス中ボタンドラッグをカメラのパンに割り当て（左=回転/中・右=パン/ホイール=ズーム）。中ボタンの自動スクロールを抑止。
- docs/（goals, plan, progress, changelog）を整備。CLAUDE.md に「作業ごとに docs を更新する」ルールを追加。
- `data/` ライブラリ機能を追加：生成時に `data/` へ自動保存（`<id>.u16` / `<id>.preview.png` / `library.json`）、
  一覧表示・クリックで再表示・削除に対応（`src/main/library.ts`）。
- 左の大領域を「位置選択 / 2D / 3D」の 3 タブ UI に再構成。生成後は 3D ビューへ自動切替。
- エクスポートを「選択中ライブラリアイテムを PNG16 / R16 で書き出し」に変更（`.u16` から都度生成）。
- 3D 地形ビューワ（Three.js）を追加：ドラッグ回転・ズーム・高さ強調スライダー・標高別配色（`src/renderer/viewer3d.ts`）。
- エクスポートの既定保存先を `data/` に統一。`data/` を `.gitignore` に追加。
- `start.bat` を ASCII 化（CP932 環境の文字化け修正）。起動失敗時に `pause` で残すよう改善。
- 起動クラッシュ修正：preload を `.cjs` 出力に、`app.getPath` をモジュール最上位から遅延呼び出しへ。
- 初期実装：Electron + TS 雛形、タイル取得・合成、RGB→標高デコード、16bit PNG / R16 出力、
  2D 地図（MapLibre）での範囲選択、2D グレースケールプレビュー。
