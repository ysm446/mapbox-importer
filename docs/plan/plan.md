# 計画 / ロードマップ

## 完了済み（詳細は progress.md / changelog.md）
- Electron + TS の雛形、タイル取得・標高デコード・16bit 出力
- 2D 地図での範囲選択、2D グレースケールプレビュー
- 3D 地形ビューワ（Three.js、実寸高さ・グリッド・カメラフィット）
- 衛星画像テクスチャの取得・保存・3D メッシュ貼り付け（切替可）
- `data/` ライブラリ（自動保存・一覧・選択・削除）と左 3 タブ UI
- 3D ランドマーク（地点）：`<id>.annotations.json` に lng/lat で保存、3Dにリーダー線＋ラベル、3Dクリック配置＋数値編集、標高は自動サンプリング＋手入力
- ランドマーク追加：座標範囲変更後のツェルマットエリアにゴルナーグラート周辺・5-Seenweg周辺のランドマークを追加
- 共通ランドマークライブラリ：Git管理するマスターを `assets/landmarks/landmark-library.json` に置き、`data/landmark-library.json` をローカル作業コピーとして初回生成
- 3D 等高線：表示/間隔/単色設定に加え、単色/グラデーション切替とバー型キーポイントUIによるグラデーション編集
- UI ステータスバー：画面下部に常設し、生成・更新・読み込み・入出力・スクリーンショットの進捗と結果を表示
- ルートフォルダ：ロケーションの保存先を切替可能にし（`userData/config.json` の `rootDir`/`recentRoots`）、環境設定は `userData/settings.json` へ共通化。テーマ別にデータを分離できる
- ランドマーク追加：2026年ネパール大規模洪水ロケーションに吉隆口岸（中国側検問所）、氷河崩落地点、ランタン・リルンを追加

## 進行中
- **OSM ルート／カーブ**
  - [x] フェーズA：Overpass から道路/歩道/鉄道を bbox（地形と同じ矩形）で取得→2D オーバーレイ→クリックで除外→採用を `Workspace.routes`（lng/lat 折れ線）に保存。`src/main/osm.ts`。
  - [x] フェーズB（ドレープ）：保存ルートを 3D 地形表面に沿わせて `THREE.Line` で描画（`viewer3d.ts` の `setRoutes`/`renderRoutes`、`surfaceWorldY` で接地、種別色は 2D と一致）。「表示」メニューに「ルートを表示」トグル。
  - [x] フェーズB残：**カーブ単位の距離・勾配ラベル**（1ルート1ラベル。例「1.4km/18%」を線の中点に表示）。実装済み（`viewer3d.ts` の `buildRouteLabel`/`routeLabels`、declutter 相乗り＋オンスクリーン長 LOD＋長さ優先度、「表示」メニューに「距離・勾配」トグル）。
    - **距離**＝水平（地図）距離。Web Mercator の緯度補正を入れる。
    - **勾配**＝②平均グレード＝`総上り / 水平距離`（登り基調を見る用途。下りは無視）。
    - **計算と保存**：`setRoutes` のドレープ計算と同じパスで `{距離, 勾配, 中点アンカー}` を1回算出し**メモリにキャッシュ**（`Route.id` キー、地形更新・ルート変更で破棄）。`workspace.json` には保存しない（距離は coords だけで出るので保存不要、勾配は地形依存で陳腐化するため）。ルート一覧パネルにも同キャッシュから表示できる。
    - **フォント**：地名ラベルより小さめにする。
    - **アンカー**：弧長で二分した中点を地表から少し浮かせて配置。画面外なら非表示（可視区間の中点へ寄せると尚良）。
    - **カリング／重なり回避**：地名ラベルの declutter にルートラベルも相乗りさせる（重なり回避・フェード・優先度を共用）。加えて (1) オンスクリーン長 LOD＝ルートの画面上の長さが一定 px 未満なら非表示、(2) 優先度＝総距離が長い順（混雑時に主要ルートを残す）、(3) ルート単位 AABB フラスタムカル。
    - 未実装（v1 では見送り）：(3) ルート単位 AABB フラスタムカル（現状は全ラベルを毎フレーム投影。数千本で重ければ追加）／モーフ中のラベル位置追従（今は新地形の中点に固定でフェードイン）／ラベルテクスチャの遅延生成（今は setRoutes で全ルート分を一括生成）。距離/勾配のルート一覧パネル表示も未。

- **経路探索**（実装済み・v1）
  - [x] 3Dビューにスタート/ゴールのマーカーを出し、ドラッグで移動→離すたびに再探索。`viewer3d.ts` の `setRouteSearchMode`/`setRouteSearchPoints`/`setRouteSearchPath`。
  - [x] グラフ復元＝頂点を量子化キー（1e-7度）で weld。OSM の交差点は同一ノード＝同一座標なので繋がり、立体交差は座標が違うので繋がらない。`src/renderer/routing.ts`。
  - [x] スナップ＝最寄り線分への垂線の足。両端ノードを始点/終点集合にしてダイクストラ法（CSR＋二分ヒープ）。
  - [x] 対象種別は既定で道路・歩道・登山道（鉄道/ロープウェイはOFF）。ルートパネルで切替。
  - [x] 勾配はフル解像度 u16 から算出（バッチIPC `workspace:sampleElevations`）。
  - 未実装（v2 以降の候補）：労力ベースのコスト関数（Tobler/Naismith 等。`RouteGraph.weights` の生成箇所を差し替えるだけで載る）／探索結果を `Route` として保存するボタン／発着位置の `workspace.json` への永続化／経路の断面図（標高プロファイル）。

## 次の候補（優先度順・未着手）
2. **Web Mercator の緯度歪み補正**
   - 高緯度で縦に伸びる比率を補正（メッシュの縦横比 or リサンプル）。
3. **標高レンジの手動指定**
   - min/max を固定して複数タイルの段差を揃える（現在は自動 min/max 正規化）。
4. **出力フォーマット追加**
   - TIFF / EXR。Houdini 向けに 32bit float も検討。
5. **配布**
   - `npm run dist`（electron-builder / nsis）でインストーラを作る。
6. **地点（ラベル）の種類分け＋重要度**
   - `Landmark` に種類（category）/重要度（priority）を1フィールド追加（既存は既定値、`RouteCategory` パターンを再利用）。
   - **declutter を重要度優先に**：現在はカメラ距離だけで前後を決めているのを「重要度→距離」順にし、山頂・主要地点を優先して残す（一番の実利）。
   - 種類ごとの色/アイコン/マーカー形状（例：▲山頂・●集落・⛺小屋）と、種類別の表示フィルタ（ルートの種別トグルと同じUI）。
   - 「建物」は当面**点ラベルの一種類**として扱うに留める。建物の輪郭（footprint）/3D化は大型機能でツール目的（ハイトマップ生成・プレビュー）から外れるため、具体的な必要が出たら別途検討（保留）。
   - 進め方：まず種類＋重要度フィールドと declutter 優先度から着手し、色/アイコン/フィルタは段階的に。

## 演出の改善メモ
- [x] **ハイト・モーフ時のラベルを自然に整える**：ルート折れ線・地点（マーカー＋線＋ラベル）・軸ラベル(km)を、まるごと非表示ではなく**地形変形に追従＋フェードイン**するようにした（`viewer3d.ts` の `LandmarkFollow`/`AxisFollow`/`routeLines`、`startMorph`/`updateTransition`/`finishTransition`）。grid は従来どおり X/Z 伸縮。
  - 残：ワークスペース切替で「消える旧地点」は morph が単一メッシュで旧グループを即破棄するためフェードアウトできていない（新地点のフェードインのみ）。必要なら旧注釈だけ別管理して薄く残す拡張余地あり。

## アイデア（保留・優先度低）
- **レイマーチ・ボリュメトリック雲（影付き）**
  - 高度スラブ（例：地表+2000m ≒ `2000*WORLD_SCALE` ワールド単位）にFBMノイズを密度としてレイマーチ。太陽方向への二次マーチで自己影＋地形への落ち影をつけると一気に雰囲気が出る。
  - 技術的には可能。要追加：シーンを深度テクスチャ付き RenderTarget に描く→フルスクリーンの雲シェーダで合成→最後にギズモ、という小さなポストパイプライン（現状は `animate()` で直接 render）。`time` uniform で流れる雲。
  - 操作系：表示ON/OFF・雲量・密度・高度・速さ・太陽方向（`settings.json` に追加）。
  - **トレードオフ**：そこそこ重い（半解像度＋ステップ制限＋ブルーノイズで実用域）。やり過ぎると DCC ツール的になり、本来の「ハイトマップ生成・プレビュー」から目的がぶれる点に注意。やるなら軽量トグルとして割り切る。

## ランドマーク機能メモ
- 座標は lng/lat で保存（メッシュのダウンサンプリングや再生成に非依存）。3D描画時に bbox からローカルXZへ変換。
- 標高は配置時に全解像度 `<id>.u16` からサンプリング（`annotations:sampleElevation`）。手入力で上書き可。
- リーダー線・ラベルは depthTest 無効で地形に隠れず常に見える（添付の登山地図風）。

## 技術メモ / 注意点
- preload は sandbox 対応のため `.cjs` で出力している。
- main プロセスで `app.getPath` をモジュール最上位で呼ばない（起動クラッシュ回避）。
- `.bat` は ASCII で書く（CP932 環境での文字化け回避）。
- 大きい範囲はタイル数上限 400 で制限している（`src/main/index.ts`）。
