# 進捗状況

- 2026-08-30：2026年ネパール大規模洪水ロケーションにランタン・リルンを追加（山頂標高7234mを採用、保存済みDEM約6918mとの急峻地形由来の差を確認）。

最終更新: 2026-08-31（3Dビューに比較モードを追加：ライブラリの比較ボタンで他ロケーションの地形を主地形の東側に実寸比で並べ、全体フィット・チップ帯での解除に対応（比較地形にロケーション名ラベルは出さない）。2026年ネパール大規模洪水ロケーションに吉隆口岸（中国側検問所）と氷河崩落地点を追加。3Dビューと位置選択の地図で Shift 押下中にマウス位置の標高をツールチップ表示する機能を追加（Terrain-RGB タイルを直接デコード、タイルキャッシュ付き）。位置選択の地図オーバーレイに座標貼り付け欄を追加。Google マップでコピーした座標（10進／度分秒／共有URL）を貼り付けるとその地点へ移動しマーカーを表示。3Dビューでルート線をクリックすると一覧の該当行へジャンプして強調表示するようにし、ロケーション詳細の一覧（ランドマーク／ルート）だけがスクロールして操作部は固定されるようにUIを調整。3Dで掴める対象を開いているサブタブに合わせて絞り、ランドマークのピンはランドマークタブ、経路探索の発着はルートタブでのみドラッグできるようにした。3Dビューに経路探索モードを追加：発着マーカーをドラッグすると、取り込み済みルート網の上で最短経路を引き直し、距離・累積上り下り・平均勾配を表示する。探索は `src/renderer/routing.ts`（純粋モジュール）でグラフ復元＋ダイクストラ法。勾配算出用にバッチ標高IPC `workspace:sampleElevations` を追加。プロジェクト全体のコードレビューを実施し、見つかったバグを一括修正：ロケーション選択の競合（連続クリック・await 中の選択変更で別ロケーションへ保存され得る問題）、クリップ分割された OSM ルートの id 重複（1本削除で全消し）、等高線グラデーションの Delete キー誤発火、bbox ドラッグのスタック、未捕捉 Promise 拒否、3Dビューワの衛星テクスチャ誤破棄・遷移後の軸ラベル表示・morph 中の NaN 座標・ファークリップ等。メイン側は rangeMin/rangeMax 正規化範囲のメタ不整合と satellite:fetch のタイル数上限を修正し、`@types/three` を devDependencies へ移動。CLAUDE.md を実態（docs/plan/ 配下のパス・ディレクトリ構成・ルートフォルダ）に合わせて更新。画面固定サイズのラベル表示をルートの距離/勾配ラベルにも適用。ロケーションの保存先を切替可能な「ルートフォルダ」にし、環境設定を userData 共通へ移した。`data/` の23ロケーションを `E:\sample files\location-viewer\` の japanese-history / japanese-mountains / swiss へ分割コピー。`countOutsideLandmarks()` の `selectedBbox` ナローイング不備による既存の型エラー `TS2345` を修正し、`npx tsc --noEmit` がクリーンに通る状態へ戻した。画面下部に常設ステータスバーを追加し、各アクションの進捗・結果・待機状態を表示。共通ランドマークライブラリのマスターを `assets/landmarks/landmark-library.json` に移し、Git管理・配布同梱できるようにした。ツェルマットエリアにゴルナーグラート等のランドマーク9件を追加し、外部ソースと保存済みDEMで確認。ルンガーンエリアにランドマーク9件を追加し、外部ソース・保存済みDEM・一部は保存済み衛星画像/既存OSM形状で確認。等高線の初期値ボタンをカメラ画角リセットと同じ回転矢印アイコンボタンに変更。等高線の単色カラー入力とグラデーションバーの横に初期値ボタンを追加し、それぞれ既定色/既定ストップへ戻せるようにした。等高線グラデーションの選択中キーポイントをDelete/Backspaceキーで削除できるようにし、追加/削除ボタンを削除。等高線グラデーションのキーポイントつまみから突起を削除し、ひし形のみの見た目に調整。等高線グラデーションのキーポイントドラッグ中にハンドルDOMを再生成せず、対象ハンドルの位置・入力値・バー背景だけを局所更新してドラッグが即解除される問題を修正。等高線グラデーションUIで、キーポイントのドラッグ中はバーのみ更新し、3D等高線の再生成と保存はドラッグ終了後に行うよう調整。単色/グラデーションの選択に応じて未選択側の設定要素を確実に隠すよう修正。等高線の色モードに単色/グラデーション選択を追加し、バー型キーポイントUIでグラデーションストップを編集・保存・復元できるようにした。F12キーで現在のアプリウィンドウを `data/screenshot/` にPNG保存できるスクリーンショット機能を追加。座標範囲変更後の空木岳エリアに南側稜線ランドマーク5件を追加し、外部ソースと保存済みDEMで確認。ランドマークタブに範囲外ランドマークの一括削除ボタンを追加。タブ配色を調整し、アクティブタブをパネルと同色・非アクティブタブを明るめに変更。空木岳エリアにランドマーク8件を追加し、外部ソースと保存済みDEMで確認。プロジェクト名/アプリ表示名を `Location Viewer` に更新。OSMルート取得・表示カテゴリにロープウェイ（aerialway）を追加。浄土平エリアの高山・鳥子平の座標を外部ソースと保存済みDEMで再確認して修正。3Dランドマークの地名ラベル内で標高行だけを小さく表示するよう調整。環境設定タブをセクション分けし、等高線セクションに色設定を追加。3Dビューの表示メニューで `グリッドを表示` を先頭に移動。3Dビューに等高線表示チェックと等高線間隔設定を追加。3Dビューの表示メニューにグリッド表示チェックを追加。3Dビューの表示メニューに地形（ハイトフィールド）表示チェックを追加。検証済みランドマークライブラリから生成時トレース用の sourceWorkspaces/sourceLandmarkIds を削除。アルブラ峠・ベルニナ峠・アイガー北壁・フルカ峠・ユリア峠・ゴッタルド峠・ツェルマット由来ランドマーク122件を外部ソースとDEMで確認しhigh化。ルート距離/勾配ラベルの距離計算を水平距離/実距離で切替できる環境設定を追加。浄土平・立山・御嶽山・乗鞍岳・白馬岳・上高地・北岳・谷川岳由来ランドマーク142件を外部ソースとDEMで確認しhigh化。3Dランドマークの描画順をラベル優先に調整。富士山・茶臼岳・木曽駒ヶ岳由来ランドマーク55件を外部ソースとDEMで確認しhigh化。八ヶ岳・天狗岳由来ランドマーク30件を外部ソースとDEMで確認しhigh化。既存data内21ロケーションのランドマークから共通ランドマークライブラリ365件を生成。共通ランドマークライブラリとbbox内候補の取り込みUIを追加。八ヶ岳ロケーションに桜平駐車場（上）（中）を追加。天狗岳ロケーションに桜平駐車場（中）（下）を追加。OSMタブの取得・種別再判定・全クリアを同じ操作列に配置。標高タイルのズーム上限をソースごとに反映し、Terrain-DEM の z15 取得失敗を防止。位置選択の生成パネルからアプリ名見出しを削除。位置選択にタイルグリッド表示トグルを追加。ロケーション切り替え時に地図の fitBounds 余白を広げ、矩形が画面いっぱいに詰まらないよう調整。作業ごとに docs/ を更新する運用ルールを AGENTS.md に追加）

## 現在の状態
MVP + 3D ビューワ + `data/` ライブラリまで動作。型チェック・本番ビルド・dev 起動すべて成功。
実機で 4547×3783px の生成・保存も確認済み。

## 完了
- [x] Electron + TS 雛形（electron-vite）、`start.bat`（ASCII）
- [x] タイル座標計算・並列ダウンロード（`src/main/tiles.ts`）
- [x] タイル合成・標高デコード・16bit/raw 出力・メッシュ生成（`src/main/heightmap.ts`）
- [x] 2D 地図（MapLibre）での範囲選択（表示範囲／矩形ドラッグ）、地図スタイル切替、解像度推定（px/MP/タイル数）
- [x] ライブラリ選択で対象 bbox へ地図移動（fitBounds）・ズーム復元
- [x] 2D グレースケールプレビュー（ズーム/パン/ダブルクリックでフィット）
- [x] 3D 地形ビューワ（Three.js、実寸高さ・グリッド・地形配色・中ボタン=パン・高さ強調1〜10×・衛星テクスチャ・方位ラベル/軸ギズモ）（`src/renderer/viewer3d.ts`）
- [x] 環境設定を `data/settings.json` に保存（地図スタイル・言語）
- [x] UI 多言語化（日本語/英語）と地図ラベルの言語連動（`src/renderer/i18n.ts`）
- [x] 衛星画像を `data/<id>.satellite.png` に保存し 3D メッシュに貼り付け、チェックボックスで切替（WebP をレンダラー Canvas で合成）
- [x] `data/` ライブラリ：生成時に自動保存・一覧・選択再表示・削除（`src/main/library.ts`）
- [x] 3カラムUI（左=設定 / 中央=位置選択・2D・3Dタブ / 右=ライブラリ）
- [x] トークンを userData に保存、`data/` を `.gitignore`
- [x] リファクタリング：座標変換を `src/shared/mercator.ts` に集約、3Dメッシュのダウンサンプリング処理を共通化（挙動変更なし）
- [x] PNG16 書き出しを 16bit RGBA（R=G=B＋pHYs）形式に変更（参照ハイトマップ互換）
- [x] 3D ランドマーク（地点）フェーズ1：3Dクリック配置・リーダー線/ラベル表示・数値編集（lng/lat 保持）
- [x] データ管理をワークスペースモデルへ再設計（`data/<id>/` フォルダ構成、地形は更新可能、地点はワークスペースに紐づく、旧構成は自動移行）
- [x] OSM ルート フェーズA：Overpass から道路/歩道/鉄道を bbox 取得→2D 地図にオーバーレイ→クリックで除外→採用を `Workspace.routes` に保存（`src/main/osm.ts`）
- [x] OSM ルート フェーズB（ドレープ）：保存ルートを 3D 地形表面に沿わせて描画（`viewer3d.ts` の `setRoutes`/`renderRoutes`）、表示トグル `showRoutes`
- [x] ロケーションの ZIP バックアップ書き出し／取り込み（復元）：外部依存なしの最小 ZIP（`src/main/zip.ts`）。1 ロケーション＝1 ZIP（workspace.json/heightmap.u16/preview.png/satellite.png）。取り込みは新しい id を採番して新規ロケーション化
- [x] 位置選択：ロケーション切り替え時の地図フィット余白を画面サイズに応じて広げ、矩形が画面いっぱいに詰まらないよう調整
- [x] 位置選択：選択中ズームレベルのタイル境界を薄いグリッドで表示するトグルを追加（パン・ズーム・スタイル切替・ズームスライダーに追従）
- [x] 位置選択：生成パネル先頭のアプリ名見出しを削除し、地図上オーバーレイを簡潔化
- [x] 位置選択：標高ソースごとのズーム上限を起動時・ソース変更時・ロケーション選択時に反映し、上限超過時は生成前に分かりやすく止める
- [x] OSMタブ：`OSMから取得`、`種別を再判定`、`全ルートをクリア` を同じ操作列に配置
- [x] 天狗岳ロケーション：桜平駐車場（中）（下）のランドマークを追加（`data/` 配下の生成データ）
- [x] 八ヶ岳ロケーション：桜平駐車場（上）（中）のランドマークを追加（`data/` 配下の生成データ）
- [x] 共通ランドマークライブラリ：`data/landmark-library.json` と、選択中ロケーションのbbox内候補をランドマークタブから取り込むUIを追加
- [x] 共通ランドマークライブラリ：マスターを `assets/landmarks/landmark-library.json` に移し、`data/landmark-library.json` はローカル作業コピーとして扱うように変更
- [x] 共通ランドマークライブラリ：既存 `data/*/workspace.json` の21ロケーションからランドマークを集約し、365件の `landmark-library.json` を生成
- [x] 共通ランドマークライブラリ：八ヶ岳・天狗岳由来のランドマーク30件を外部ソースと保存済みDEMで確認し `confidence: high` に更新
- [x] 共通ランドマークライブラリ：富士山・茶臼岳・木曽駒ヶ岳由来のランドマーク55件を外部ソースと保存済みDEMで確認し `confidence: high` に更新
- [x] 共通ランドマークライブラリ：浄土平・立山・御嶽山・乗鞍岳・白馬岳・上高地・北岳・谷川岳由来のランドマーク142件を外部ソースと保存済みDEMで確認し `confidence: high` に更新
- [x] 共通ランドマークライブラリ：アルブラ峠・ベルニナ峠・アイガー北壁・フルカ峠・ユリア峠・ゴッタルド峠・ツェルマット由来のランドマーク122件を外部ソースと保存済みDEMで確認し `confidence: high` に更新
- [x] 共通ランドマークライブラリ：検証後のJSONから生成時トレース用の `sourceWorkspaces` / `sourceLandmarkIds` を削除
- [x] 3Dランドマーク：マーカー・リーダー線・ラベルの描画順を整理し、重なったときはラベルがマーカーより前に出るよう調整
- [x] 3Dルート：距離/勾配ラベルの距離計算を環境設定で `水平距離` / `実距離（斜距離）` から選択できるように追加
- [x] 3Dビュー：表示メニューに `地形を表示` チェックを追加し、ハイトフィールドの表示/非表示を切替・保存できるように追加
- [x] 3Dビュー：表示メニューに `グリッドを表示` チェックを追加し、グリッドと端の距離ラベルの表示/非表示を切替・保存できるように追加
- [x] 3Dビュー：表示メニューに `等高線を表示` チェック、環境設定に `等高線の間隔` セレクトを追加
- [x] 環境設定：一般/Mapbox/3Dビュー、および3Dビュー内の表示・カメラ/注釈/ルート/等高線にセクション分けし、等高線の色設定を追加
- [x] 環境設定：3Dビューの表示・カメラに背景色カラーピッカーと初期値アイコンを追加し、`backgroundColor` として保存・復元できるように追加
- [x] 3Dビュー：等高線の色を単色/グラデーションで切替でき、バー型キーポイントUIでグラデーションストップを編集・保存・復元できるように追加
- [x] 3Dビュー：ランドマークラベルの標高行を地名より小さく表示するよう調整
- [x] ランドマーク：浄土平エリアの高山・鳥子平を再確認し、保存済みDEM標高と整合する座標へ修正
- [x] OSMルート：`aerialway=*` をロープウェイカテゴリとして取得・2D/3D表示・表示切替できるように追加
- [x] 名称：パッケージ名・製品名・ウィンドウタイトル・READMEを `Location Viewer` に更新
- [x] ランドマーク：座標範囲変更後の空木岳エリアに南駒ヶ岳・赤梛岳・田切岳・摺鉢窪避難小屋・仙涯嶺を追加
- [x] ランドマーク：ルンガーンエリアにルンガーン・ルンガーン駅・旧教会塔・ルンガーン湖・カイザーストゥールOW駅・ヴィラーホルン・ギュプフィ・オプゼー・ルンガーン＝トゥレン山麓駅を追加
- [x] ランドマーク：ツェルマットエリアにゴルナーグラート・ゴルナーグラート駅・ローテンボーデン駅・リッフェルアルプ駅・リッフェルホルン・グリンジゼー・グリューンゼー・モースジゼー・ウンターロートホルンを追加
- [x] スクリーンショット：F12キーで現在のアプリウィンドウを `data/screenshot/` にPNG保存
- [x] 3D：画面固定サイズのラベル表示をルートの距離/勾配ラベルにも適用
- [x] データ分離：ルートフォルダ（データフォルダ）の切替と最近使ったフォルダの履歴
- [x] データ分離：環境設定 `settings.json` を `userData/` へ移し全ルート共通化（旧 `data/settings.json` は自動移行）
- [x] データ分離：`data/` の23ロケーションを japanese-history / japanese-mountains / swiss へ分割コピー
- [x] 型：`countOutsideLandmarks()` の `selectedBbox` ナローイング不備による `TS2345` を修正
- [x] UI：画面下部にステータスバーを常設し、各アクションの進捗・結果と待機状態を表示
- [x] UI：右サイドバーのスクロールをタブ内容エリア内に移し、タブ見出しを固定表示に変更
- [x] UI：右サイドバーのタブ内容スクロールバーをペイン右端へ寄せ、内容側には右余白を残すよう調整
- [x] 運用ルール：まとまった作業ごとに `docs/` を更新する最重要ルールを `AGENTS.md` に追加
- [x] レビュー：プロジェクト全体をレビューし、選択競合によるデータ混入・OSMルートid重複・グラデDeleteキー誤発火・bboxドラッグのスタック・未捕捉Promise・3Dビューワのテクスチャ破棄/遷移ラベル/morph NaN/ファークリップ・rangeMin/Max メタ不整合・satellite:fetch 上限なし等を修正
- [x] docs：CLAUDE.md の docs 参照パス（`docs/plan/`）・ディレクトリ構成・保存先（ルートフォルダ）の記述を実態に合わせて更新
- [x] 経路探索：3Dビューで発着マーカーをドラッグ→最寄りルートへスナップ→ダイクストラ法で最短経路→距離・累積上り下り・平均勾配を表示（`src/renderer/routing.ts`、バッチ標高IPC `workspace:sampleElevations`）
- [x] ルート：3Dでルート線をクリック→一覧の該当行へスクロール＋強調表示（`userData.routeIds` で線分→ルートidを逆引き）
- [x] UI：ロケーション詳細の一覧（ランドマーク／ルート）だけをスクロール領域にし、サブタブ見出しと操作ボタンを固定表示に
- [x] 3D操作：掴める対象を開いているサブタブに限定（ランドマークのピン＝ランドマークタブ、経路探索の発着＝ルートタブ）。ランドマークタブを離れたら配置モードも解除

## 未着手 / 次の一手
- [ ] OSM ルート：区間距離(km)/勾配(%)ラベル（ドレープは実装済み、ラベルが未）
- [ ] Web Mercator 緯度歪み補正
- [ ] 標高レンジ手動指定 / TIFF・EXR 出力 / 配布インストーラ

## 既知の注意
- サンドボックス開発環境では `ELECTRON_RUN_AS_NODE=1` により起動できない（実機は無関係）。
- 3D メッシュはプレビュー用に最大 256 頂点へダウンサンプリング（保存データはフル解像度）。
- Mapbox 衛星タイルは常に WebP 返却。main(pngjs) では合成できないためレンダラー Canvas で合成する。
- 衛星画像は新規生成分のみ。旧ライブラリ項目には `satellite.png` が無い（再生成が必要）。

## コミット状況
- ここまでの全実装は `main` にコミット済み（最新: 座標変換集約・メッシュ処理共通化のリファクタリング）。未 push の変更はなし（`.claude/settings.json` のローカル設定差分を除く）。
